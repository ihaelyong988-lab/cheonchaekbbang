# AGENTS.md — 천책빵 작업 규약

이 문서는 저장소 `C:/dev/cheonchaekbbang` 의 최상위 작업 규약이다. 상위 사용자 헌법(`~/.claude/CLAUDE.md`)의 하위 규칙이며, 충돌 시 상위 헌법이 우선한다.

사실 원천은 2026-07-25 1차 방문자 실사 산출물 3종이다. 수치를 기억이나 추정으로 쓰지 않는다.

- 순번 원장 62건 + 요약·만족도·디자인 판정·배치 계획: `scratchpad/p1-ledger.json`
- 8차원 실측 원문: `scratchpad/p1-measurements.json`
- 발견 집계(발굴 경로 수치는 §10-0): `scratchpad/p1-stats.json`
- 경로 접두: `C:/Users/이해룡/AppData/Local/Temp/claude/C--dev/f0c3201c-0f1f-4818-8a6e-0f70a19f6cca/`

이 문서가 인용하는 행 번호는 모두 실제 파일을 열어 확인한 값이다. 코드를 고치면 같은 커밋에서 이 문서의 행 번호도 갱신한다.

---

## §0 자기개선·반복금지

### 0-1 착수 게이트

작업 착수 전 다음 순서를 지킨다. 이 순서를 건너뛴 작업은 마감 대상이 아니다.

1. 이 문서를 §0부터 끝까지 읽는다. 이 문서를 읽었다는 사실을 첫 응답에 1줄로 보고한다.
2. `docs/BACKLOG-v1.8-방문자실사.md` 와 `scratchpad/p1-ledger.json` 의 해당 항목을 조회한다. 이미 판정이 끝난 항목을 다시 조사하지 않는다.
3. 착수 보고에 `사용 스킬: …` / `미사용+사유: …` 를 1줄로 남긴다.
4. 편집 대상 파일과 그 파일을 감시하는 기계 게이트(§0-3 표)를 먼저 특정한다.

### 0-2 규약 6조

- **R1 한 번 = 영구.** 사용자가 한 번 설명한 지시·선호·교정은 그 턴 안에 이 문서의 해당 절로 승격한다. 두 번 묻지 않는다.
- **R2 근본 원인까지 규칙화.** 교정을 받으면 증상이 아니라 원인을 조문으로 박는다. 같은 계열 재발 0이 목표다.
- **R3 오류는 그 턴 안에 §9 등재.** 사용자 교정뿐 아니라 작업 중 스스로 겪은 도구 실패·검증 실패·잘못된 1차 접근·환경 함정을 전부 §9에 남긴다. 기록 없는 오류 1건은 마감 불가 사유다.
- **R4 리마인더가 아니라 기계 채점.** 규칙은 권고문으로 쓰지 않는다. grep 가능한 패턴, 수치 임계, 단언 문장 중 하나의 형태로만 쓴다. 채점 불가능한 문장은 규칙이 아니라 메모다.
- **R5 확률패턴보다 지시맥락.** 임의 판단 지점에서는 통계적 기본값이나 "보통 이렇게 한다" 보다 현재 지시·이 문서·원장·대화 흐름을 먼저 검토한다. 맥락 검토 후에도 불확정이면 만들기 전에 before→after 1줄로 확정한다.
- **R6 1정보 1표시.** 한 화면·한 문서에서 같은 수치·라벨·문장을 두 번 표시하지 않는다. 원천 1곳에서만 렌더하고 다른 곳에서는 참조한다.

### 0-3 마감 게이트

아래 5항 전부 통과가 마감 조건이다. 하나라도 미통과면 마감을 보고하지 않는다.

1. `npm test` 5종 전량 통과(`package.json` scripts: test:data / test:search / test:static / test:mobile / test:features).
2. 편집한 파일을 감시하는 게이트를 같은 커밋에서 갱신했다. 파일과 게이트 대응은 다음과 같다.

| 편집 파일 | 동반 확인 게이트 |
| --- | --- |
| `sw.js` | `tests/static-rules.mjs`(자산 목록 · 캐시 버전 · 갱신 규칙) · `tests/production-smoke.mjs` 의 `assets["sw.js"]` 캐시 버전 표식 |
| `index.html` | `tests/static-rules.mjs:26-29`(4탭·순서) · `:43-50`(메타) · `:76-79`(브랜드 버튼) |
| `app.js` | `tests/static-rules.mjs:31-34`(홈 순서) · `:58` · `:72-73` · `:80-83` · `:86-94` |
| `app.css` | `tests/static-rules.mjs:63-64`(그라데이션·이모지) · `:67-68` · `:78-79` · `:95-97` |
| `data/*.js` | `tests/data-integrity.mjs` 전량 · `tests/mobile-navigation.mjs:152-160` · `:162-190` |
| `lib/search.js` | `tests/search-quality.mjs` 전량 |

3. 어떤 배포 커밋이든 `sw.js` 의 `CACHE` 버전을 상향하고, 그 값을 하드코딩한 게이트 **2곳을 모두** 동반 갱신했다 — `tests/static-rules.mjs` 의 캐시 버전 단언과 `tests/production-smoke.mjs` 의 `assets["sw.js"]` 표식이다(근거 §9 E-002 · E-015). 채점: `grep -rn "ccb-v" sw.js tests/` 결과의 버전 문자열이 전부 같은 값이다.
4. 이번에 겪은 오류를 §9에 전부 등재했다.
5. 산출물 안에서 같은 수치가 두 번 나타나지 않는다(R6). 문서 산출물은 §0-4 표를 기준으로 자기점검한다.

### 0-4 수치 단일 출처 표 (R6 집행용)

이 문서 안에서 각 수치군을 쓸 수 있는 절은 하나뿐이다. 다른 절에서는 절 번호로만 참조한다.

| 수치군 | 단일 출처 절 |
| --- | --- |
| 카탈로그 규모(도서·질문·분야·여정 수) | §1 INV-8 |
| 고객 만족 4핵심 충족률과 목표치 | §2-3 |
| 색 토큰 값·대비 실측값·폰트 크기·간격 | §4 |
| 저장 용량·문자 상한·정화 결과 | §5 |
| 히스토리 인덱스·팝업 조건 | §6 |
| 성능 실측치와 예산치 | §7 |
| 접근성 임계값 | §8 |

---

## §1 앱 핵심 목적·불변식

천책빵은 천 권의 책을 지식의 계보(뿌리→줄기→가지)로 배열하고, 답이 아니라 질문을 입장점으로 삼는 독서 대시보드다. 방문자는 첫 화면에서 오늘의 질문 한 문장을 받고, 그 질문이 나온 책으로 들어가고, 그 책의 뿌리를 거슬러 올라간다. 계정도 서버도 없다. 이름과 기록은 방문한 기기에만 남는다. 부제는 "뿌리를 찾는 서재"이며 앱 이름은 "천책빵"으로 고정한다.

아래 불변식은 바꾸면 이 앱이 이 앱이 아니게 되는 항목이다. 변경은 사용자 명시 승인과 게이트 동시 갱신 없이는 금지한다.

- **INV-1 입장점은 질문이다.** 홈 첫 화면 최상단은 히어로 질문 카드다(`app.js:498-514`). 홈 정보 순서는 히어로 → 질문 검색 → 최근 질문 → 질문 여정 → 읽는 중 → 분야별 진행이다. (게이트: `tests/static-rules.mjs:31-34`, `tests/mobile-navigation.mjs:131`, `:144-149`)
- **INV-2 계보 3계단과 roots[0] 체인은 단일 진실이다.** 계단은 root/trunk/branch 3종이며 한국어 표기는 뿌리/줄기/가지다(`app.js:32-33`). 뿌리까지의 거리 계산은 `stepsToRoot`(`app.js:38-51`) 하나만 사용한다. 모든 비뿌리 책은 `roots[0]` 체인으로 뿌리에 도달해야 한다. (게이트: `tests/static-rules.mjs:86-88`, `tests/data-integrity.mjs:30-42`, `:72-74`)
- **INV-3 서버·계정·외부 API 없음.** 네트워크 호출 코드를 두지 않는다. UI 문구에 인증 어휘를 쓰지 않는다(§2-4). (게이트: `tests/static-rules.mjs:65`, `:66`, `tests/mobile-navigation.mjs:249-250`)
- **INV-4 4탭 구성과 순서 고정.** 홈 → 계보 → 서재 → 기록(`index.html:36-53`). 탭 추가·삭제·재배열 금지. (게이트: `tests/static-rules.mjs:26-29`, `tests/mobile-navigation.mjs:386`)
- **INV-5 오프라인 완전 동작.** 전 자산을 install 시 프리캐시하고(`sw.js:3-16`, `:18-20`) fetch 는 cache-first 다(`sw.js:36-41`). 오프라인 재로드에서 홈·히어로 질문·서재 전량이 렌더돼야 한다. (게이트: `tests/static-rules.mjs:52-56`, `tests/mobile-navigation.mjs:134`, `:145-147`)
- **INV-6 첫 화면 복귀는 goHome 단일 진입점이다.** 상단 브랜드 버튼(`index.html:27`)과 홈 탭(`app.js:858`)은 같은 `goHome()`(`app.js:316-327`)으로만 연결된다. 별도 복귀 경로를 만들지 않는다. (게이트: `tests/static-rules.mjs:80-83`, `tests/mobile-navigation.mjs:302-328`)
- **INV-7 개인 기록은 기기 로컬에만 남는다.** 저장소는 `localStorage` 키 `cheonchaek.v1` 하나다(`app.js:54`). 프로필 이름은 `textContent` 로만 출력한다. 기록을 외부로 전송하는 코드는 금지한다. (게이트: `tests/static-rules.mjs:72`, `:73`, `tests/feature-regression.mjs:188-189`)
- **INV-8 카탈로그 규모는 단일 출처를 따른다.** 현행 실측은 도서 175권, 질문 590문항, 분야 6종, 여정 6종(분야당 1개)이다. 분야 확장 목표 합계는 1,000권이다. 이 값을 바꾸는 변경은 데이터 게이트와 카탈로그 단언을 같은 커밋에서 갱신한다. (게이트: `tests/data-integrity.mjs:15`, `:62`, `:78-79`, `tests/mobile-navigation.mjs:152-160`)
- **INV-9 히어로 질문 박스는 2줄 고정이다.** 질문 길이에 따라 글자 크기만 압축하고 박스 높이는 변하지 않는다(`app.css:121-148`). 질문 전수에서 2줄 초과 0건을 유지한다(압축 단계 수치는 §4-4). (게이트: `tests/mobile-navigation.mjs:162-190`)
- **INV-10 종료 의사 없이 앱을 벗어나지 않는다.** 히스토리 최초 항목은 종료 트랩 센티널이다(`app.js:203-206`). 팝업 표시 중 반복 뒤로가기로 앱을 이탈하지 않는다. (게이트: `tests/mobile-navigation.mjs:352-356`)

---

## §2 방문자 Flow 계약

사용자가 지정한 여정을 이 앱의 실제 구조로 매핑한 계약이다. 각 계약문은 검증 가능한 단언으로 쓴다. "현재" 열의 원장 번호는 `p1-ledger.json` 의 순번이다.

### 2-1 단계별 계약

| 단계 | 계약문 (반드시 지켜야 하는 것) | 현재 |
| --- | --- | --- |
| ① 방문 | C1-1 첫 화면은 홈 탭이며 히어로 질문 카드가 최상단에 있다. C1-2 진입 URL 의 해시가 탭을 지정하면 그 탭으로 부팅하고 URL 을 재기입하지 않는다. C1-3 재방문 시 직전 히어로 질문을 반복하지 않는다. | C1-1·C1-3 충족. C1-2 위반(원장 6) |
| ② 내 서재 등록 | C2-1 등록은 계정이 아니라 이 기기의 로컬 프로필이다. 이름 1필드, 길이 상한은 §5-1, 서버 왕복 0회. C2-2 기록 0건 첫 방문자에게 등록 수단과 보관 범위를 첫 화면에서 1회 고지한다. C2-3 이름 저장·변경·삭제 후 상단 버튼 라벨이 즉시 반영된다. | C2-1·C2-3 충족. C2-2 위반(원장 7) |
| ③ 관심사항 등록 | C3-1 방문자가 표현한 관심(수집한 질문·읽음 상태·검색어·선택한 분야)은 표현 즉시 상태에 반영된다. C3-2 한국어 입력이 조합 단계에서 파괴되지 않는다. C3-3 보유 도서를 정확한 제목·저자로 검색하면 결과가 1건 이상이다. | C3-1 부분. C3-2 위반(원장 1). C3-3 위반(원장 15·16·17·18) |
| ④ 맞춤설정 | C4-1 설정은 한 곳(설정 오버레이)에 모인다: 테마·관심 분야·서재 필터·기록 관리. C4-2 설정 변경은 즉시 영속화된다. C4-3 영속 대상 필드는 `sanitizeState` 화이트리스트에 등재된다. | C4-1 위반(원장 8). C4-2·C4-3 부분(테마 1종만 영속, 원장 5) |
| ⑤ 재방문 맞춤화면 | C5-1 계보 분야·서재 분야·서재 계단 필터는 마지막 선택을 복원한다. C5-2 검색어와 스크롤 위치는 복원하지 않는다(세션 한정). C5-3 읽음·수집 질문·답변·여정 진행은 100% 복원된다. C5-4 같은 기기 두 탭이 동시에 열려 있어도 먼저 탭의 기록이 소실되지 않는다. | C5-2 충족. C5-3 은 카탈로그가 동일할 때만 충족(카탈로그 변경 시 원장 11). C5-1 위반(원장 5). C5-4 위반(원장 3) |
| ⑥ 기록 초기화 | C6-1 앱 안에 기록 전체 삭제 수단이 있다. C6-2 삭제는 이름뿐 아니라 답변·수집 질문·여정 진행까지 함께 지운다. C6-3 삭제 후 다음 저장에서 지운 데이터가 되살아나지 않는다(in-memory 상태 재초기화 필수). C6-4 삭제는 실행 전 1회 확인을 받는다. | C6-1부터 C6-4까지 전부 위반(원장 8) |
| ⑦ 종료 팝업 | C7-1 종료 의사 표시(첫 화면에서 뒤로가기)에만 팝업이 뜬다. C7-2 팝업은 포커스를 계속 읽기 버튼에 두고 배경 4영역을 inert 로 잠근다. C7-3 ESC·계속 읽기로 원래 화면과 포커스로 복귀한다. C7-4 닫힘 화면은 막다른 길이 아니다. | C7-1 위반(원장 4·6·12·13). C7-2·C7-3 충족. C7-4 위반(원장 38) |

### 2-2 계약 위반 판정 절차

각 계약문은 §3의 해당 차원 절차로 재현한다. 계약 위반 1건은 원장 등급 "상" 판정 근거가 된다. 계약을 만족하는데 품질만 낮은 항목은 "중" 이하로 분리한다.

### 2-3 고객 만족 4핵심

현재 충족률은 `p1-ledger.json` 의 `satisfaction` 값이며, 목표치와 판정식은 이 문서가 정한다.

| 핵심 | 현재 | 목표 | 목표 달성 판정식 (기계 채점) |
| --- | --- | --- | --- |
| 방문 목적 달성(질문으로 입장해 책에 닿기) | 40% | 85% | 대표 질의 50건 중 부당한 0건 ≤ 2건 && 2글자 제목·저자 23종 전량 ≥ 1건 && 서재 한글 IME 조합 3/3 정상 |
| 맞춤형 셋업 | 15% | 70% | 영속 설정 필드 ≥ 4종(theme·lineageDomain·libDomain·libTier) && 설정 진입점 1개 && 기록 초기화 버튼 1개 |
| 자기인식 | 45% | 80% | 홈 "나의 기록" 4칸 전량이 방문자 실적 기준(랜덤 히어로 통계 0칸) && 저장되지만 노출 0인 필드 0종 |
| 관심사항 추적 | 20% | 70% | 방문자 표현 행위 5종(입력 질문·계보 분야·서재 필터·서재 검색어·현재 탭) 중 영속 ≥ 3종 && 읽음 이력이 히어로 질문 분포에 반영(가중 > 1.0) |

### 2-4 어휘 금지 계약

UI 문구(`index.html`·`app.js` 생성 문자열)에 다음 어휘를 쓰지 않는다: 로그인, 본인인증, 인증번호, SMS, 휴대폰 인증. 이 앱에는 계정이 없으므로 해당 어휘는 사실과 다르고 방문자에게 서버 인증을 오인시킨다. 전화 입력 필드(`input[type=tel]`, `autocomplete=tel`)도 금지한다.

게이트: `tests/static-rules.mjs:66`(정적 소스 정규식), `tests/mobile-navigation.mjs:249-250`(렌더 후 DOM 문구·입력 필드 0건). 새 안내 문구를 추가할 때는 이 두 게이트를 먼저 확인한다.

---

## §3 MECE 8차원 점검표

다음 실사는 이 표만 보고 같은 측정을 반복할 수 있어야 한다.

### 3-0 공통 전처리 (모든 브라우저 실측의 선행 조건)

- **P-1 서버.** `node:http` 정적 서버, 응답 헤더 `Cache-Control: no-store`. 참조 구현 `tests/feature-regression.mjs:28-40`.
- **P-2 캐시 초기화.** 첫 로드 후 `serviceWorker.getRegistrations()` 전량 unregister + `caches.keys()` 전량 삭제 → `reload`. 저장 계층 측정은 `localStorage.clear()` 를 함께 실행한다. 참조 `tests/mobile-navigation.mjs:120-127`, `tests/feature-regression.mjs:55-62`. 이 전처리를 빠뜨리면 구 캐시가 응답해 수정 전 코드를 측정한다(§9 E-002).
- **P-3 뷰포트 명시.** 기본 3폭을 명시 지정한다: 375x812(모바일) · 768x1024(태블릿) · 1440x900(데스크톱). 회귀 게이트용 폭은 360/390/430(`tests/mobile-navigation.mjs:366`). 뷰포트 미지정 측정은 무효로 본다.
- **P-4 종료.** 모든 프로브는 `browser.close()` + `server.close()` 로 끝낸다. 좀비 프로세스를 남기면 다음 측정이 오염된다.
- **P-5 저장소 무변경.** 프로브 스크립트와 출력물은 스크래치패드에만 쓴다. 측정 후 `git status --porcelain` 이 공백임을 확인한다.
- **P-6 playwright import.** 스크래치패드에서 `import { chromium } from "playwright"` 는 실패한다. `file:///C:/dev/cheonchaekbbang/node_modules/playwright/index.mjs` 절대 URL 로 import 한다(§9 E-004).

### 3-1 D1 진입·성능

- 측정 대상: 콜드 FCP·DCL·load·첫 질문카드 페인트, 재방문(SW 캐시 적중) 동일 지표, 스크롤 프레임 간격, 롱태스크, 모듈 체인 홉 수와 전송량, 더 보기 렌더, 오프라인 부팅.
- 절차: P-1부터 P-6까지 적용 후 (a) 무제한 (b) 4G(9Mbps·85ms RTT)+CPU4x (c) Fast-3G(1.6Mbps·562ms RTT)+CPU4x 3조건에서 각 3회 측정하고 중앙값을 기록한다. 첫 질문카드 페인트는 `.q-text` 노드 출현 시점으로 정의한다. 스크롤은 서재 전량 렌더 상태에서 300px×60회 이동하며 프레임 간격 p50/p95/max 와 32ms 초과 프레임 수를 센다.
- 판정: §7 예산표.

### 3-2 D2 등록·맞춤화 Flow

- 측정 대상: §2-1 7단계 계약 전량, 저장소 덤프(저장되는 필드 / 저장 안 되는 필드), 재방문 복원·리셋 대조표, 개인화 반영도.
- 절차: (a) 0클릭 상태 저장소 덤프 (b) 이름 등록 → 질문 수집 → 답변 입력 → 분야 선택 → 필터 적용 후 덤프 (c) 재로드 후 각 항목 복원 여부 1:1 대조 (d) 특정 분야 전량 읽음 상태로 20회 재방문해 히어로 질문 분야 분포를 무가중 기대치와 비교 (e) 375px 더블탭 간격 0/40/60/80/150/250ms 와 1440px 마우스 더블클릭으로 닫기 버튼 관통을 시험한다.
- 판정: §2-1 계약 위반 0건, §2-3 목표 달성.

### 3-3 D3 내비게이션·백링크

- 측정 대상: 4탭×오버레이 5종 전이 행렬, 뒤로/앞으로 데스싱크, 히스토리 누적, 탭 전환 시 스크롤, 백링크 착지 위치, 종료 팝업 조건, 해시 딥링크.
- 절차: 탭 전환마다 `history.state`·`location.hash`·렌더된 탭 3값을 함께 기록한다. 홈→계보→서재→기록 진행 후 뒤로 2회·앞으로 3회를 실행하고 URL 변경 횟수와 화면 변경 횟수를 비교한다. 백링크는 착지 요소의 `getBoundingClientRect().top` 과 뷰포트 내 가시 여부를 기록한다. 오버레이 개폐 5회 반복 후 인덱스 증가량을 확인한다.
- 판정: §6 계약 위반 0건. URL 변경 횟수와 화면 변경 횟수가 불일치하면 위반이다.

### 3-4 D4 데이터 지속성·무결성

- 측정 대상: 저장 페이로드 크기, 저장 시점, 용량 초과 거동, 카탈로그 드리프트 거동, 다중 탭 경합, 손상·적대적 페이로드 내성, 초안 유실 매트릭스.
- 절차: (a) 페이로드 바이트와 키 목록을 0클릭 시점부터 기록 (b) `localStorage` 를 채워 헤드룸을 0에 수렴시켜 실제 저장 실패를 유도한다(수렴 단계와 총량 채우기가 실패하는 이유는 §9 E-005) (c) 카탈로그에서 기록 보유 도서를 제거한 빌드로 로드해 read/reading/questions/journey 변화와 안내 유무를 기록 (d) 두 탭을 열고 A 탭 조작 → B 탭 조작 → 저장 결과를 대조 (e) 손상 JSON·타입 불일치·`__proto__` 오염·초과 길이·여정 결번 등 16종을 주입해 pageerror 와 프로토타입 오염을 확인 (f) 초안은 시트 이탈·탭 전환·재로드·pagehide·visibilitychange 5경로로 보존 여부를 표로 만든다.
- 판정: §5 불변식 위반 0건.

### 3-5 D5 콘텐츠·검색 품질

- 측정 대상: 질문 문구 품질(절단 비문 비율·중복·길이), 검색 재현율, 조사 커버리지, 주제어 사전 커버리지, 계보 분포.
- 절차: 데이터 모듈을 node 로 직접 import 해 전수 계산한다(브라우저 불필요). 판정 규칙은 두 개를 함께 쓴다: (a) 인용부가 원문의 접두이면서 어절 중간에서 끊긴 항목 (b) 인용부가 원문 접두이면서 종결어미로 끝나지 않는 항목. 검색은 대표 질의 50건 이상, 2글자 제목·저자 전수, 1글자 등록 주제어, 조사 변형(에·도·만·이나·라도 포함)을 각각 호출해 0건 응답을 분류한다. UI 계약(입력 최소 길이, 빈 결과 안내, 정규화)은 DOM 실측으로 교차 확인한다 — 엔진 단위 테스트만으로 UI 도달성을 보증하지 않는다.
- 판정: 부당한 0건 비율과 절단 비문 비율은 §2-3 방문 목적 달성 판정식에 종속한다.

### 3-6 D6 디자인 적합성

- 측정 대상: 폰트 크기 선언 종수와 실렌더 종수·노드 분포, 텍스트·비텍스트 대비, 중복 표시, 간격 실측, 클리핑·가로 넘침.
- 절차: 4탭·오버레이 5종·2테마 전수를 렌더해 텍스트 노드의 계산된 `font-size` 를 수집한다. 대비는 전경/배경 실제 계산색으로 산출하며 `opacity` 가 걸린 노드는 합성 후 색으로 계산한다. 중복 표시는 같은 수치·라벨이 무스크롤 동시 가시인지로 판정한다. 간격은 `.section-label` 상단 실측 간격의 distinct 값 수를 센다.
- 판정: §4 규칙표.

### 3-7 D7 접근성·입력

- 측정 대상: 액션 후 포커스, 탭 순회, 포커스 트랩, aria-live 낭독, 한글 IME, 터치 타깃, 접근 이름, 200% 확대, reduced-motion.
- 절차: 본문 액션 7종과 오버레이 액션 전량에서 액션 직후 `document.activeElement` 를 기록한다. IME 는 CDP `Input.imeSetComposition` 3단계로 조합을 구동하고 최종 입력값과 결과 수를 기록한다(대조군은 재렌더가 없는 입력 필드). 200% 확대는 375px 기기 기준 188x406 뷰포트로 대체 측정하고 `documentElement.scrollWidth > clientWidth` 를 가로 넘침 판정으로 쓴다.
- 판정: §8 임계표.

### 3-8 D8 안정성·에러

- 측정 대상: `npm test` 5종, 콘솔 전수 스윕, 저장 실패 경로, 히스토리 미지 state, 대량 렌더 동기 시간.
- 절차: 전 화면·전 조작 스윕에서 console error/warning/pageerror 를 수집한다. 성능 판정은 Playwright wall-clock 이 아니라 페이지 내부 `performance.now()` 로 동기 렌더 구간만 측정한다(§9 E-003). 히스토리는 `location.hash` 직접 변경, 세션 복원, SW 갱신 3경로에서 `history.state` 와 팝업 표시 여부를 함께 기록한다.
- 판정: 런타임 오류 0건, §6 팝업 금지 조건 위반 0건.

---

## §4 디자인 시스템

근거: `p1-ledger.json` 의 `designVerdict`, `p1-measurements.json` 의 D6 실측, `app.css` 현행 선언.

### 4-1 컬러 토큰

| 토큰 | 은회(기본) | 남색(`html[data-theme="navy"]`) | 용도 |
| --- | --- | --- | --- |
| `--silver` | `#E4E4DF` | `#0F2A43` | 상단바·뿌리 카드 표면 |
| `--silver-line` | `#D2D2C9` | `#36536C` | 은회 표면 경계 |
| `--hero-surface` | `#D6D6CF` | `#173A55` | 히어로 질문 표면 |
| `--hero-accent` | `#704600` | `#E2BF7A` | 히어로 위 라벨 |
| `--ink` | `#26282A` | `#F5F2EA` | 본문 |
| `--paper` | `#F5F5F1` | `#10263A` | 앱 배경 |
| `--charcoal` | `#3A3C39` | `#2456A6` | 주 행동 버튼 면 |
| `--bronze` | `#8F5A0E` | `#E2BF7A` | 라벨·강조 텍스트·포커스 링 |
| `--amber` | `#B4832F` | `#D2A451` | 진행 그래픽 |
| `--line` | `#E0E0D8` | `#36536C` | 카드 경계 |
| `--muted` | `#5A5B55` | `#C2CDD6` | 보조 텍스트 |
| `--card` | `#FFFFFF` | `#18334B` | 카드 표면 (은회 값은 위반, 4-5 참조) |
| `--canvas` | `#DFDFD8` | `#091A2A` | 앱 외부 배경 |

선언 위치는 `app.css:2-18`(은회)·`:20-34`(남색)이며, 토큰 추가·삭제 시 두 블록을 같은 커밋에서 갱신한다. 하드코딩 색은 금지한다 — 예외는 4-5에 열거된 3개 값이며 이들은 제거 대상이다.

### 4-2 대비 미달 지점

임계값은 §8-1 이 단일 출처다(WCAG 1.4.3 텍스트, WCAG 1.4.11 비텍스트). 아래는 그 임계에 대한 실측 미달 목록이며 값은 은회/남색 순이다.

텍스트:

| 지점 | 실측 | 원인 |
| --- | --- | --- |
| 수집됨(disabled) | 2.45 / 3.44 | `app.css:213` `opacity: 0.45` |
| 여정 잠금 `tier-branch` | 2.48 / 3.19 | `app.css:394` `opacity: 0.58` |
| 여정 잠금 `card-meta` | 2.63 / 3.77 | 같음 |
| 여정 잠금 `tier-trunk` | 3.53 / — | 같음 |
| 여정 잠금 `card-title` | 3.84 / 4.99 | 같음 |

비텍스트:

| 지점 | 실측 | 원인 |
| --- | --- | --- |
| 분야 게이지 채움/트랙 | 1.26 / 2.28 | `app.css:255-256`, `:448` |
| 게이지 트랙/카드 | 1.13 | `app.css:248`, `:255` |
| 여정 진행바 채움/트랙 | 2.84 | `app.css:259-260` |
| `btn-light` 면/히어로 | 1.46 → 1.10 | `app.css:205` |
| `btn-light` 테두리/히어로 | 1.04 → 1.47 | 같음 |
| `btn-outline` 테두리/히어로 | 1.35 → 5.99 | `app.css:206` 은회 고정값 |

규칙: 비활성 상태를 `opacity` 로 표현하지 않는다. 비활성은 전용 토큰 색으로 표현하고 임계를 통과시킨다. 기계 채점 — `app.css` 의 `opacity` 선언 중 텍스트를 포함한 요소에 적용된 것 0건.

### 4-3 타이포 6단 스케일

확정 스케일: 12 / 13 / 15 / 17 / 20 / 24px. 숫자는 전역 `tabular-nums` 를 적용한다.

| 단 | 용도 | 현행 값 매핑 |
| --- | --- | --- |
| 12 | 캡션·뱃지·탭 라벨·섹션 라벨 | 10.5 · 11.5 · 12 · 12.5 |
| 13 | 카드 메타·보조 문구·버튼 라벨 | 13 · 13.5 · 14 |
| 15 | 본문·카드 제목·입력 | 14.5 · 15 · 15.5 · 16 |
| 17 | 수치 강조·소제목 | 16.5 · 17 · 17.5 · 18 |
| 20 | 히어로 질문 상단·시트 제목·브랜드 | 19 · 20 |
| 24 | 예비 (현행 미사용) | — |

현행은 `app.css` 선언 17종 + `app.js:766` 인라인 1종이며 실렌더 15종/496 노드다. 이 중 9종·424노드(85.5%)가 11.5px 이상 15.5px 이하 4px 폭에 몰려 인접 단계 차이가 0.5px(3.3%에서 4.3% 사이)로 지각 임계에 미달한다. 14.5 · 16 · 16.5px 는 각 2노드짜리 1회성 값이다.

기계 채점 — `app.css` 의 `font-size` 선언 집합은 위 6값만 허용한다(`grep -o "font-size: [0-9.]*px" app.css | sort -u` 결과 6행). `app.js`·`index.html` 의 인라인 `font-size` 선언은 0건이어야 한다.

### 4-4 히어로 질문 압축 (불변식)

압축은 20 / 17 / 15px 3단이며 하한은 15px 로 고정한다. 13px 단계는 폐기한다. 근거: 렌더 폭 324px·박스 58px 기준으로 3단 적용 시 질문 전수 2줄 초과 0건(span 최대 56px)이며, 13px 단계는 박스 유지에 불필요함이 실측으로 확정됐다. 현행 4단에서는 250건(42.4%)이 13px 로 압축되어 본문·카드 제목·스트립 수치보다 작아지는 위계 역전이 발생한다.

임계 하드코딩이 두 곳에 중복되어 있다: `app.js:489` 의 클래스 매핑과 `tests/mobile-navigation.mjs:180` 의 버킷 값. 단일 출처로 뽑아 같은 커밋에서 갱신한다.

기계 채점 — `app.css` 의 `.q-text` 계열 `font-size` 선언은 3종이며 최솟값이 15px 이다. INV-9 의 2줄 게이트를 함께 통과해야 한다.

### 4-5 진행바 토큰 단일화와 순백 금지

진행바 채움 색이 3종으로 갈려 있다: `#E7D6B8`(`app.css:256`) · `#8A7655`(`:448`) · `var(--amber)`(`:260`). 트랙도 `#F1F1EA`(`:255`)와 `#ECECE3`(`:259`)로 갈렸다. `--progress-fill` / `--progress-track` 2토큰으로 통일하고 두 테마 모두 §8-1 비텍스트 임계를 통과시킨다.

`.btn-outline` 테두리만 테마 토큰이 아닌 은회 고정값 `#B9B9AE`(`app.css:206`)이라 테마 전환만으로 히어로 두 버튼의 시각 우선순위가 뒤집힌다. 토큰화한다.

순백 금지: 표면 토큰에 `#FFFFFF` 를 쓰지 않는다. 현행 `--card: #FFFFFF`(`app.css:14`)가 위반이며, 게이지 트랙 경계 소실(4-2)의 부수 원인이다. 어두운 면 위 텍스트 색으로서의 흰색은 허용한다(`app.css:451`).

기계 채점 — `app.css` 의 `--*` 표면 토큰 선언에 `#FFFFFF`·`#fff` 0건. 진행바 색 리터럴(`#E7D6B8`·`#8A7655`·`#F1F1EA`·`#ECECE3`) 0건. `#B9B9AE` 0건.

### 4-6 간격 리듬

세로 리듬 토큰은 4 / 8 / 12 / 16 / 20 / 24px 6종이다. `.section-label` 상단 간격은 선행 요소 종류와 무관하게 단일 값이어야 한다. 현행 선언은 `app.css:117` 단일 값이지만 실측은 22 / 22 / 32 / 32px 4종으로 갈린다(`.card` 가 `button` 이라 마진 접힘이 일어나지 않는다). `.card + .section-label` 규칙을 추가해 값을 수렴시킨다.

기계 채점 — 4탭·오버레이 5종 렌더에서 `.section-label` 상단 실측 간격의 distinct 값이 1개. 컨테이너 패딩 값은 위 6종만 허용한다.

---

## §5 데이터 무결성 불변식

### 5-1 `validateBooks` / `sanitizeState` 가 보장하는 것

`validateBooks`(`app.js:6-29`)는 tier/roots 불일치, principle·questions 공란, 비뿌리 `root_reason` 공란, 뿌리 도달 실패(고아·순환)를 제거한다. 현행 카탈로그에서 제거 대상은 0권이며 `console.warn` 도 0건이다.

`sanitizeState`(`app.js:64-109`)는 다음을 보장한다.

- 존재하지 않는 도서 id 제거, 중복 제거(`app.js:60-62`)
- `read` 와 `reading` 교집합 제거 — 읽음이 우선(`app.js:67`)
- 질문 항목은 유효 id·중복 제거 후 통과(`app.js:69-70`)
- 답변 문자열 상한 10,000자 절단(`app.js:75`, `:95`)
- 프로필 이름 trim 후 20자 절단(`app.js:97`)
- `theme` 화이트리스트 2종(`app.js:104`), `questionDeck`·`lastHeroQuestionId` 유효 id 검증(`app.js:105-107`)
- §3-4 절차 (e) 의 적대적 페이로드 주입에서 pageerror 0건, 프로토타입 오염 0건

### 5-2 보장하지 못하는 것 (구멍)

- **미지 필드 소실.** `version: 99`·`3` 페이로드도 전부 `version: 2` 로 재기록되며 스키마 밖 필드(notes·highlights·profile.avatar)는 사라진다. 마이그레이션 경로가 없다.
- **카탈로그 드리프트 시 무통보 삭제.** 카탈로그에 없는 id 는 로드 직후 폐기·영속화된다(확정 시점 실측은 §10-2 원장 11). 여정 진행은 첫 결번에서 잘린다(`app.js:82-85`).
- **질문 id 불안정.** id 는 `bookId#배열인덱스`(`app.js:56-58`)이므로 질문 배열 순서가 바뀌면 같은 id 가 다른 질문을 가리켜 답이 엉뚱한 질문에 붙는다.
- **다중 탭 병합 없음.** `save()`(`app.js:125-133`)는 메모리 상태 전체를 덮어쓰고 `storage` 이벤트를 청취하지 않는다.
- **저장 실패 무통보.** 실패 통보는 `announce()`(`app.js:121-124`) → `#app-status`(`index.html:57`) 한 곳이며 `app.css:436-439` 로 1x1px clip 이라 시각 사용자에게 보이지 않는다.
- **용량 한도 도달 가능.** 오리진 한도 실측 약 524만 자에 대해 앱 허용 최대는 질문 589개 × 답변 상한 = 589만 자다.
- **입력 상한 무경고.** 10,500자를 입력하면 화면에는 전량이 남고 저장은 상한값에서 절단되어 500자가 경고 없이 사라진다(`textarea` 에 `maxlength` 없음).

### 5-3 불변식

- **DI-1 격리 원칙.** 카탈로그 드리프트로 참조를 잃은 방문자 기록은 삭제하지 않고 `state.orphans` 로 격리 보존한다. 격리 항목이 존재하면 기록 탭에서 1회 고지한다. 방문자 기록을 소리 없이 지우는 코드는 금지한다.
- **DI-2 질문 id 안정성.** 질문 id 는 배열 인덱스에서 파생하지 않는다. 데이터에 고정 `qid` 를 두고 그것만 저장 키로 쓴다. 질문 문구를 고쳐도 id 는 유지한다.
- **DI-3 상한 단일 출처.** 5-1의 답변 상한은 상수 1개로 정의하고 `sanitizeState`·입력 위임(`app.js:956-968`)·`textarea maxlength` 가 같은 상수를 참조한다. 상한 도달 시 남은 글자 수를 보여준다.
- **DI-4 낙관적 UI 금지.** `save()` 반환값을 무시하는 호출부를 두지 않는다. 실패 시 직전 상태를 롤백하고 보이는 오류를 렌더하기 전에는 성공 UI(수집됨·읽음 배지 등)로 전환하지 않는다.
- **DI-5 병합 쓰기.** `save()` 는 현재 저장값을 읽어 필드 단위로 병합한다. `storage` 이벤트를 청취해 타 탭 변경을 상태에 반영한 뒤 렌더한다. 병합 후에도 `version` 은 현행 버전을 유지한다.
- **DI-6 마이그레이션 경로.** 저장 버전을 올릴 때는 이전 버전 페이로드를 변환하는 함수를 같은 커밋에 둔다. 미지 상위 버전은 덮어쓰지 않고 격리한다.
- **DI-7 로컬 외 전송 금지.** 어떤 경로로도 기록을 외부로 보내지 않는다. 내보내기는 `Blob` + `a[download]` 로컬 파일로만 구현하고, 불러오기는 `sanitizeState` 를 반드시 통과시킨다(INV-3·INV-7).

게이트: `tests/feature-regression.mjs:161-190`(정화 결과 단언, `:179` 버전), `:193-206`(저장 실패 통보), `tests/static-rules.mjs:73`(답변 상한 방어). DI-1·DI-2 채택 시 `tests/feature-regression.mjs:178-190` 을 같은 커밋에서 갱신한다.

---

## §6 내비게이션·히스토리·종료 팝업 계약

### 6-1 스택 구조

- 히스토리 최초 항목은 종료 트랩 센티널이며 그 다음 항목이 기본 화면이다. 뷰의 단일 출처는 `history.state` 에 직렬화된 페이로드이고, 앱은 별도 뷰 배열을 두지 않는다(포인터 + 직렬화 방식 — 잘라내기 경로를 원천 제거한다).
- **N-1 센티널은 값으로 식별한다.** 센티널 판정은 `{ sentinel: true }` 고유 값으로만 한다. `history.state` 가 없는 항목(null)을 센티널과 같은 값으로 접으면 해시 이동·북마크·세션 복원만으로 종료 팝업이 뜬다. (구현 완료: 원장 13)
- **N-2 pushState 페이로드에 뷰를 직렬화한다.** `{ i, view: { tab, overlay } }` 를 실어 `popstate` 에서 뷰를 복원한다. 인덱스만 저장하고 앞쪽 항목을 잘라내면 앞으로가기 복원이 불가능하다. (구현 완료: 원장 14)
- **N-3 미지 state 는 재구성한다.** 값 식별에 실패한 항목은 팝업을 띄우지 않고 재구성한 뒤 `replaceState` 로 인덱스를 재기입한다. 재구성 기준은 현재 주소의 해시를 먼저 보고, 실패하면 현재 뷰다 — 해시가 이미 새 탭으로 옮겨진 상태에서 현재 뷰만 쓰면 URL 과 렌더 탭이 어긋난 채 굳는다. (구현 완료: 원장 13)
- **N-10 딥링크는 2항목으로 부팅한다.** 인덱스 0 = 홈, 인덱스 1 = 요청 탭. 단일 항목으로 하면 `goHome` 이 요청 탭에서 벗어나지 못하고 진입 직후 첫 뒤로가기가 §6-3 을 위반한다. (구현 완료: 원장 6)
- **N-4 오버레이 개폐는 히스토리를 누적하지 않는다.** 오버레이 개폐 5회 반복 후에도 인덱스는 불변이다.
- 기계 채점: 탭 전환 12회 후 인덱스가 선형 증가하고, 뒤로가기 횟수와 화면 변경 횟수가 1:1로 일치한다. URL 만 바뀌고 화면이 고정되는 구간 0건.

### 6-2 종료 팝업이 떠야 하는 조건

- 첫 화면(인덱스 0, 오버레이 없음)에서 뒤로가기 1회. (게이트 `tests/mobile-navigation.mjs:228-233`)
- `goHome()` 복귀 직후 뒤로가기 — 브랜드 버튼 경로(`:311-312`), 홈 탭 경로(`:324-325`), 오버레이 경유 후 복귀 경로(`:346-347`).
- 팝업 표시 중 반복 뒤로가기에서는 팝업이 유지되고 인덱스와 URL 이 불변이다(`:352-356`, INV-10).

### 6-3 종료 팝업이 떠서는 안 되는 조건

- 같은 문서의 해시만 바뀐 경우(주소 재입력·북마크·공유 링크·세션 복원). 현행 위반(원장 13).
- 서비스워커 갱신 직후. 현행 위반(원장 12).
- 해시 딥링크로 진입한 직후의 첫 뒤로가기. 현행 위반(원장 6).
- 오버레이 닫기 액션이 호출한 `history.back()`. 프로필 저장·이름 지우기·여정 완료의 더블탭이 팝업을 띄우면 위반이다(원장 4).
- 탭 전환 뒤로가기의 중간 단계(게이트 `tests/mobile-navigation.mjs:224-230` 이 팝업 비표시를 단언한다).
- 기계 채점: 위 5경로 각각에서 `#exit-dialog` 가시 = false.

### 6-4 오버레이 닫힘 단일 경로

- **N-5 닫힘은 단일 함수를 경유한다.** 배경 탭(`app.js:918-920`), 닫기 버튼, ESC(`app.js:353-356`), 저장 후 닫기(`app.js:852`·`:856`·`:880`)를 하나의 `dismissOverlay()` 로 통합한다. `history.back()` 직접 호출 지점은 그 함수 1곳으로 제한한다.
- **N-6 재진입 가드는 popstate 도착 시 해제한다.** 해제 지점은 `popstate` 핸들러 최상단(`app.js:251` 직후)에 둔다. 조기 return 경로(`app.js:253-259`)에 두면 플래그가 고착된다. `history.back()` 은 비동기이며 지연 실측값은 §10-2 원장 4 에 있다.
- **N-7 닫힘 직후 배경 입력 잠금.** 오버레이 해제 직후 약 150ms 동안 `.tabbar` 에 `pointer-events: none` 을 적용한다. `inert` 는 쓰지 않는다(키보드 포커스를 차단하면 §8 위반).
- **N-8 상태 변경 액션은 중복 실행되지 않는다.** 여정 완료 등 기록을 추가하는 핸들러는 진입부에 중복 가드를 둔다.
- 기계 채점: 동일 태스크 내 2회 호출과 실포인터 더블탭 간격 0/80/150/250ms 에서 기록 증가 1건, 팝업 비표시, 탭 이탈 0건.

### 6-5 해시 딥링크 복원

- 부팅 시 `location.hash` 를 `HASH`(`app.js:204`) 역매핑으로 조회해 초기 뷰 스택과 첫 `pushState` 대상 탭을 결정한다. 미지 해시는 홈으로 폴백한다. 요청한 해시를 다른 값으로 재기입하지 않는다.
- 오버레이는 하위 탭과 해시가 같아 주소로 구분되지 않는다. 복원 범위는 탭 단위로 한정한다.
- 기계 채점: `/#library` 진입 → 렌더 탭 = 서재 && 최종 URL = `#library`. 해시 없는 진입은 `#question` 유지(기존 게이트 `tests/mobile-navigation.mjs:129`·`:235` 보존).

### 6-6 탭 전환 스크롤

- **N-9 탭 전환은 최상단에서 시작한다.** 탭이 바뀌는 `pushView` 경로에서만 스크롤을 0으로 되돌린다. 오버레이 push 에는 적용하지 않는다 — 오버레이 개폐 시 배경 스크롤 보존은 정상 동작이다. (구현·게이트 완료: 2026-08-02. 그 전까지 이 조항은 문서에만 있었고 홈 탭만 `goHome` 경로로 최상단에 닿았다 — §9 E-012)
- **N-11 하단 탭은 그 화면의 첫 페이지를 연다.** 하단 탭 4개는 어느 것을 눌러도 그 화면의 첫 페이지 최상단에서 시작한다. 이미 보고 있는 탭을 다시 누르는 경우도 무동작이 아니라 첫 페이지 복귀다. 첫 페이지로 되돌리는 대상은 **누적된 목록 페이지와 펼쳐 둔 항목**뿐이다(서재 `libVisibleCount` · 기록 `openProgressDomain`). 분야·계단 필터와 검색어는 되돌리지 않는다 — 필터는 C5-1 로 복원 대상이고 검색어는 C5-2 로 세션 한정이며, 사용자가 입력한 값을 탭 한 번에 지우는 것은 복귀가 아니라 파괴다. 되돌림은 `resetTabToFirstPage` 한 곳에서만 한다. 히스토리 뒤로가기로 돌아오는 경로에는 적용하지 않는다(보던 자리로 돌아가는 것이 목적이다).
- 기계 채점: G-15 (a)·(b)·(c)·(e). 오버레이 개폐 후 배경 `scrollY` 불변은 기존 게이트가 유지한다.

### 6-7 닫힘 화면

- 닫기 폴백 화면(`app.js:372-386`)은 막다른 길이 아니어야 한다. 복귀 수단 1개를 둔다. `app.css:433` 의 `.goodbye-box .btn` 은 이 용도로 전환하거나 삭제한다(원장 38·57).
- `window.close()` 는 일반 탭에서 항상 차단되며 콘솔 경고를 남긴다. 앱 수정 후 `tests/mobile-navigation.mjs:358` 의 `window.close` 스텁을 제거한다(원장 60).

---

## §7 성능 예산

D1 실측(`p1-measurements.json`)을 근거로 정한 예산이다. 실측치는 1차 측정 시점의 사실이고 예산치는 이후 커밋이 넘지 못하는 상한이다. 측정 조건은 §3-1 절차를 따른다.

| 항목 | 1차 실측 (중앙값) | 예산 (상한) |
| --- | --- | --- |
| 콜드 FCP · 로컬 무제한 | 172ms | 250ms |
| 콜드 FCP · 4G+CPU4x | 520ms | 700ms |
| 콜드 FCP · Fast-3G+CPU4x | 1,784ms | 2,200ms |
| 첫 질문카드 페인트 · 로컬 | 491.9ms | 600ms |
| 첫 질문카드 페인트 · 4G+CPU4x | 1,585.3ms | 1,800ms |
| 첫 질문카드 페인트 · Fast-3G+CPU4x | 4,679.7ms | 5,000ms |
| 재방문 FCP · SW 적중 CPU4x | 88ms | 150ms |
| 재방문 질문카드 · SW 적중 CPU4x | 218.9ms | 300ms |
| 스크롤 프레임 간격 p95 (서재 전량) | 16.9ms | 20ms |
| 스크롤 32ms 초과 프레임 | 0건 | 0건 |
| 부팅 롱태스크 최대 · 4G+CPU4x | 546ms | 600ms |
| 부팅 롱태스크 최대 · Fast-3G+CPU4x | 1,895ms | 2,000ms |
| 상호작용 후 롱태스크 최대 | 129ms | 200ms |
| 더 보기 동기 렌더 · CPU4x | 33.4ms | 50ms |
| 서재 첫 진입 동기 렌더 · CPU4x | 13.3ms | 50ms |
| 모듈 체인 홉 수 / 전송량 | 3홉 / 약 116KB | 3홉 / 140KB |

규칙: 예산 초과는 기능 추가 사유로 면제되지 않는다. 초과가 불가피하면 초과 항목·수치·사유를 §9에 등재하고 예산표를 개정한 뒤 진행한다. 기록 탭 대량 렌더(질문 전량 수집 상태, 재측정값은 §9 E-006)는 현재 예산 대상이 아니지만 100ms 를 넘기면 예산 항목으로 승격한다.

---

## §8 접근성 기준

### 8-1 임계표

| 기준 | 임계 | 기존 게이트 |
| --- | --- | --- |
| 터치 타깃 | 최소 44x44px (가시 인터랙티브 요소 전량) | `tests/mobile-navigation.mjs:137-142`, `:389-394`, `tests/static-rules.mjs:78-79` |
| 텍스트 대비 | 4.5:1 (WCAG 1.4.3) | `tests/static-rules.mjs:67-68` (히어로 강조색 2테마) |
| 비텍스트 대비 | 3:1 (WCAG 1.4.11) — 진행바·트랙·테두리·포커스 링 | 없음 (신설 대상, §4-2·4-5) |
| focus-visible 가시 | `outline: 2px solid` + `offset 2px`, 비텍스트 임계 적용 (`app.css:440`) | 없음 (신설 대상) |
| 포커스 트랩 | 오버레이·종료 팝업에서 누수 0 (`app.js:331-369`) | `tests/feature-regression.mjs:91-93`, `tests/mobile-navigation.mjs:238-245` |
| 배경 비활성 | 오버레이 중 `.topbar`/`#view`/`.tabbar` `inert=true` + `aria-hidden` | `tests/feature-regression.mjs:89-90`, `tests/mobile-navigation.mjs:337`, `:341` |
| aria-live 낭독 | 상태 변화는 `#app-status`(`index.html:57`) 또는 결과 라이브 리전으로 낭독 | `tests/feature-regression.mjs:204` |
| 액션 후 포커스 보존 | 액션 후 `document.activeElement` 가 `BODY` 로 떨어지지 않는다 | 없음 (신설 대상) |
| 접근 이름 필수 | 입력·버튼은 `label[for]` 또는 `aria-label` 을 가진다. `placeholder` 단독 금지 | `tests/mobile-navigation.mjs:194-195` (테마 버튼) |
| reduced-motion 존중 | `reduce` 에서 애니메이션 0건. 현행은 `no-preference` 조건부 선언 2블록(`app.css:179-186`·`:454-457`) | `tests/static-rules.mjs:95-97` |
| 200% 확대 무붕괴 | 확대 상태에서 가로 스크롤 0, 텍스트 잘림 0 (WCAG 1.4.10) | `tests/mobile-navigation.mjs:133`, `:388` (100% 기준만) |

### 8-2 신설 게이트 우선순위

임계는 있으나 기계 게이트가 없는 4항(비텍스트 대비 · focus-visible 대비 · 액션 후 포커스 보존 · 200% 확대)을 게이트로 승격한다. 규칙만 있고 게이트가 없으면 반복된다(R4).

- 비텍스트 대비: 채점 문장은 §11-2 G-9 가 단일 출처다.
- focus-visible: 키보드 Tab 시 `:focus-visible` 매칭 && outline 색과 인접 배경 대비가 8-1 임계 이상(2테마).
- 포커스 보존: 본문 액션 7종과 오버레이 액션 전량에서 액션 직후 `activeElement !== document.body`.
- 확대: 188x406 뷰포트에서 `documentElement.scrollWidth <= clientWidth` && `.q-text` 잘림 0건.

측정 절차는 §3-7을 따른다. 현행 미달 실측은 §4-2에 있다.

---

## §9 오류 원장 (Error Ledger)

### 9-0 형식

한 항목은 5요소로 쓴다: **발생일 · 증상 · 근본원인 · 처방 · 재발방지 게이트.** 사용자 교정뿐 아니라 작업 중 스스로 겪은 도구 실패·검증 실패·잘못된 1차 접근·환경 함정을 전부 등재한다(R3). 같은 유형이 2회째 발생하면 규칙 미비로 판정하고 해당 절에 조문을 추가한다.

### E-001 Workflow 동시 실행 슬롯 2개 — 기수를 늘려도 병렬 이득이 없다

- 발생일: 2026-07-25 (1차 실사)
- 증상: 1차 실사 견적 30분에서 40분에 대해 실제 소요가 2시간 56분을 넘겼다.
- 근본원인: 작업 PC 가 4코어이며 Workflow 동시 실행 슬롯이 2개다. 기수를 늘려도 3기 이상은 대기하므로 병렬 이득이 없고, 견적을 "기수 × 평균 소요" 가 아니라 "기수 ÷ 2 × 평균 소요" 로 계산해야 한다.
- 처방: 착수 견적에 슬롯 수 2를 상수로 넣는다. 8차원을 8기로 돌리면 벽시계는 4배수 구간이 된다. 기수를 늘리는 대신 차원당 범위를 좁히거나 라운드를 나눈다(`p1-ledger.json` 의 `batchPlan` 3라운드 구성이 이 제약을 반영한 형태다).
- 재발방지 게이트: 병렬 작업 착수 보고에 `슬롯 2 기준 예상 벽시계 = 기수 ÷ 2 × 평균` 1줄을 포함한다. 이 줄이 없으면 착수하지 않는다.

### E-002 서비스워커 cache-first — 캐시 버전을 올리지 않으면 수정이 전달되지 않는다

- 발생일: 2026-07-25
- 증상: 코드를 고쳐도 기존 방문자 화면은 변하지 않는다. 측정 시에도 구 코드가 응답한다.
- 근본원인: `sw.js:36-41` 의 fetch 핸들러가 cache-first 이고 `sw.js:2` 의 `CACHE` 상수 버전이 캐시 무효화의 유일한 트리거다.
- 처방: 모든 배포 커밋에서 `sw.js:2` 버전을 상향하고 `tests/static-rules.mjs:57`(현재 `ccb-v1.8.0` 하드코딩)을 같은 커밋에서 갱신한다. 브라우저 실측 시에는 §3-0 P-2 전처리를 반드시 실행한다.
- 재발방지 게이트: §0-3 마감 게이트 3항. 캐시 버전 미상향 커밋은 마감 불가.

### E-003 게이트가 현행 구현을 강제한다 — 코드와 게이트를 같은 커밋에서 고쳐야 한다

- 발생일: 2026-07-25
- 증상: 개선 처방을 적용하면 CI 가 즉시 실패한다.
- 근본원인: `tests/static-rules.mjs` 가 개선 대상인 현행 구현을 정규식으로 강제한다. 대표 조항은 `:59`(`isUpdate…clients.matchAll…client.navigate` 존재 요구, 원장 12의 처방 대상), `:57`(캐시 버전 하드코딩), `:58`(SW 등록 옵션). 같은 성격의 조항이 `tests/feature-regression.mjs:178-190`(정화 결과 = 삭제 방식 강제, DI-1 처방 대상), `:204`(`#app-status` 문구 요구, 원장 10 처방 대상), `tests/mobile-navigation.mjs:180`(히어로 버킷 임계 중복)에도 있다.
- 처방: 편집 대상 파일을 특정한 직후 §0-3 표로 감시 게이트를 먼저 열어본다. 규칙 교체가 필요하면 코드와 규칙을 한 커밋에 담는다. 기존 통보 경로는 제거하지 않고 추가한다(`announce()` 유지 + 보이는 배너 추가).
- 재발방지 게이트: §0-3 마감 게이트 2항.

### E-004 스크래치패드에서 playwright 모듈 해석 실패

- 발생일: 2026-07-25
- 증상: 스크래치패드 프로브에서 `import { chromium } from "playwright"` 가 `ERR_MODULE_NOT_FOUND` 로 실패한다.
- 근본원인: `node_modules` 는 대상 저장소에만 있고 프로브는 저장소 밖에서 실행된다.
- 처방: `file:///C:/dev/cheonchaekbbang/node_modules/playwright/index.mjs` 절대 URL 로 import 한다. 브라우저 실행 파일은 `tests/feature-regression.mjs:16-26` 의 탐색 순서를 그대로 재사용한다.
- 재발방지 게이트: §3-0 P-6.

### E-005 저장 실패 재현 실패 — 헤드룸을 0으로 수렴시켜야 한다

- 발생일: 2026-07-25
- 증상: `localStorage` 를 4.5MB·5,085KB 까지 채운 1차·2차 시도에서 저장이 계속 성공해 실패 경로를 재현하지 못했다. 코드상 결함이 있는데 "재현 불가"로 판정할 위험이 있었다.
- 근본원인: 앱은 기존 키를 덮어쓰므로 증가분만큼의 헤드룸만 필요하다. 총량을 채우는 방식으로는 실패가 발생하지 않는다.
- 처방: 512KB → 64KB → 4KB → 256B → 16B 로 헤드룸을 0에 수렴시킨다. 또는 `Storage.prototype.setItem` 에 `QuotaExceededError` 를 주입한다(`tests/feature-regression.mjs:195-200` 방식).
- 재발방지 게이트: §3-4 절차 (b).

### E-006 Playwright wall-clock 을 성능 결함 근거로 쓴 오판

- 발생일: 2026-07-25
- 증상: 기록 탭 대량 렌더를 2,791ms 로 측정해 프리즈 결함으로 올릴 뻔했다. 페이지 내부 `performance.now()` 재측정에서는 동기 렌더가 최대 46ms 였다.
- 근본원인: Playwright 를 통한 측정은 RPC 왕복과 레이아웃 시간을 포함한다.
- 처방: 성능 판정 수치는 페이지 내부 타이머로 측정한 동기 구간만 사용한다. 외부 wall-clock 은 참고값으로만 기록한다.
- 재발방지 게이트: §3-8 절차.

### E-007 죽은 CSS 자동 스캔의 오탐

- 발생일: 2026-07-25
- 증상: 자동 스캔이 `q-mid`·`q-long`·`q-xlong`·`tier-*`·`is-locked`·`q-flip` 을 미사용으로 보고했다.
- 근본원인: 이 클래스들은 템플릿 문자열 보간으로 생성되므로 정적 문자열 검색에 걸리지 않는다.
- 처방: 죽은 코드 판정은 자동 스캔 결과를 마크업 대조로 검증한 뒤 확정한다. 1차 확정 결과는 `.goodbye-box .btn`(`app.css:433`) 1건이다.
- 재발방지 게이트: 죽은 코드 제거 커밋은 해당 선택자가 생성되는 템플릿 위치를 함께 보고한다.

### E-008 이질적 다건 편집을 한 에이전트에 몰면 일부가 조용히 누락된다

- 발생일: 2026-07-25 (`memory/pmis-workflow-single-file-edit.md` 선례를 이 저장소 규약으로 승격)
- 증상: 한 에이전트에 성격이 다른 편집 다건을 지시하면 일부가 반영되지 않고 보고에는 완료로 적힌다.
- 근본원인: 산출물을 목표값과 기계 대조하지 않으면 누락이 검출되지 않는다.
- 처방: 편집은 계층(입력·오버레이 / 히스토리 / 저장·맞춤화)별로 라운드를 나누고, `app.js` 는 한 라운드에 1기만 배정한다. 라운드 종료 시 지시 항목과 산출물을 1:1 재대조한다(누락 0 확인 후 보고).
- 재발방지 게이트: §0-3 마감 게이트 1·2항 + 항목별 1:1 대조 결과를 보고에 포함한다.

### E-009 인용 행 번호는 필연적으로 어긋난다 — 참고값으로만 쓴다

- 발생일: 2026-07-25 (2차 구현 라운드 전 구간)
- 증상: 이 문서가 인용한 `파일:라인` 이 대량으로 실제와 어긋났다. 2차에서 `app.js` 가 900행 이상 바뀌면서 라운드마다 20건 내외가 이동했고, 1차 감사 시점에도 9건이 틀렸다.
- 근본원인: 행 번호는 코드가 바뀌면 반드시 무효화된다. 문서에 박아 둔 값을 매번 추적하는 비용은 무한하다.
- 처방: 인용 행 번호는 **탐색 힌트**이며 근거가 아니다. 착수 전 담당 구간을 `grep` 으로 실측하고, 문서 값과 다르면 실측값으로 작업한 뒤 보고에 "문서 X → 실측 Y" 로 남긴다. 개별 행 번호를 사후 정정하려 들지 말고, 식별자(함수명·상수명·선택자)로 인용하는 쪽을 우선한다.
- 재발방지 게이트: 라운드 착수 보고에 행 번호 재확인 결과를 포함한다(미포함 시 마감 불가).

### E-010 게이트 선작성 라운드는 의도된 빨강을 만든다 — 빨강의 성격을 구분해 기록한다

- 발생일: 2026-07-25 (R2 종료 시점)
- 증상: R2 가 R3 대상 게이트를 미리 작성해 라운드 종료 시 `npm test` 가 빨강이었다. §12-1 4항만 문자적으로 읽으면 다음 라운드 착수가 막힌다.
- 근본원인: 배치 계획이 게이트 선작성을 지시하는데 마감 게이트는 전량 초록을 요구해, 두 규칙이 중간 상태에서 충돌한다.
- 처방: §12-1 4항의 전량 초록은 **2차 마감 시점**의 조건이다. 라운드 경계에서는 빨강 하나하나를 (a) 선작성된 미구현 게이트 (b) 실제 회귀 로 분류해 커밋 메시지에 근거와 함께 남기고, (b) 가 하나라도 있으면 진행하지 않는다.
- 재발방지 게이트: 라운드 커밋 메시지에 빨강 목록과 분류 근거를 적는다. 분류는 이전 커밋 트리를 `git archive` 로 펼쳐 같은 스위트를 돌려 대조한다(R3 회귀를 이 방법으로 확정했다).

### E-011 리팩터가 무해했던 좌표를 활성 컨트롤로 바꿔 관통 결함을 되살린다

- 발생일: 2026-07-25 (R3 가 유입, R4 가 해소)
- 증상: 여정 완료 더블탭의 80ms 셀이 R2 에서 초록이었다가 R3 에서 빨강이 됐다. 잠금 코드는 건드리지 않았다.
- 근본원인: 배경 잠금이 `.tabbar` 만 덮고 복원되는 오버레이 시트를 덮지 않았다. R3 이전에는 관통 좌표에 무해한 시트 배경이 있었고, 렌더 구조가 바뀌자 같은 좌표에 활성 카드가 놓였다. 잠금 범위가 **좌표 우연**에 의존하고 있었다.
- 처방: 배경 잠금은 특정 요소가 아니라 **복원되는 대상 전체**를 덮는다. 해제 시점은 시간 상수가 아니라 복원 렌더 완료 기준으로 잡는다(0ms·80ms 를 닫으면서 250ms 정상 조작을 살리려면 시간 창 확대는 답이 아니다). `inert` 는 쓰지 않는다(N-7).
- 재발방지 게이트: G-2 5모드 행렬 + 과잉잠금 방지 단언(`tapThroughReleaseGate`). 잠금이 좁아지면 전자가, 넓어지면 후자가 잡는다.

### E-012 계약만 쓰고 구현·게이트를 빠뜨린 조항이 잔존한다 — 문서에 있다고 동작하는 것이 아니다

- 발생일: 2026-08-02
- 증상: §6-6 N-9(탭 전환은 최상단에서 시작)는 "기계 채점" 문장까지 갖춘 계약이었는데, `app.js` 의 `pushView` 에는 스크롤 복귀가 없었고 그 채점 문장에 대응하는 게이트도 없었다. 홈 탭만 `goHome` 의 `pendingHomeScroll` 경로로 우연히 최상단에 닿아 있어서, 기존 게이트(홈 탭 재탭 `scrollY === 0`)는 초록인 채 결함이 10개월 가까이 살아 있었다.
- 근본원인: 2차 구현 배치 계획(§10-1)이 **원장 번호 기준**으로만 짜였다. N-9 은 원장 항목이 아니라 §6 계약으로만 존재해서 어느 라운드에도 배정되지 않았고, "규칙만 있고 게이트가 없으면 반복된다"(§11-3)는 원칙이 정작 §6 계약 자체에는 적용되지 않았다.
- 처방: §6 의 N-조항은 전부 대응 게이트 ID 를 옆에 적는다. 게이트 ID 가 비어 있는 N-조항은 미구현으로 간주하고 백로그로 올린다. 인접 조항을 만지는 커밋은 그 절의 게이트 ID 공란 여부를 먼저 확인한다.
- 재발방지 게이트: §12-4 자기점검에 "이번에 만진 절의 N-조항 중 게이트 ID 가 비어 있는 것이 있는가" 를 포함한다. G-15 (a)·(b) 가 N-9 의 공란을 메웠다.

### E-013 형제 요소를 끼워 넣으면 `:last-child` 기반 규칙이 조용히 죽는다

- 발생일: 2026-08-02
- 증상: `.progress-row:last-child { border-bottom: none }` 은 행만 6개 나열될 때만 맞는 규칙이었다. 행마다 목록 패널 `div` 를 끼우자 마지막 자식이 패널로 바뀌어 마지막 행의 구분선이 남았다. 화면이 깨지지 않고 선 하나만 남으므로 테스트로도 눈으로도 놓치기 쉽다.
- 근본원인: 위치 기반 선택자(`:last-child`)가 **형제 구성이 고정이라는 가정**을 숨긴 채 쓰였다. 마크업에 요소를 하나 끼우는 순간 그 가정이 깨진다.
- 처방: 같은 종류의 행이 반복되는 목록에는 `:last-of-type` 을 쓰고, 상태에 따라 구분선이 달라지면 그 상태를 선택자에 명시한다(`:last-of-type:not([aria-expanded="true"])`). 위치 기반 선택자를 쓸 때는 형제 구성이 바뀌면 깨진다는 사실을 주석이 아니라 선택자 자체로 드러낸다.
- 재발방지 게이트: 목록 컨테이너에 새 형제 요소를 추가하는 커밋은 그 컨테이너를 겨냥한 `:last-child`·`:first-child`·`:nth-child` 선언을 먼저 grep 한다.

### E-014 AGENTS.md 는 1회 Read 로 전량이 들어오지 않는다

- 발생일: 2026-08-02
- 증상: 이 문서를 Read 로 한 번 읽으면 약 324행에서 잘린다(전체 937행). §0-1 착수 게이트 1항의 "§0부터 끝까지 읽는다"를 문자 그대로 1회 호출로 수행하면 §6 내비게이션 계약과 §9 오류 원장을 못 본 채 착수하게 된다.
- 근본원인: 도구의 응답 토큰 상한(약 25k)이 문서 길이보다 작다. 잘렸다는 사실이 본문 안에 표시되지 않으면 다 읽었다고 착각한다.
- 처방: 착수 시 `offset`·`limit` 으로 나눠 읽거나, 먼저 `grep -n "^## §\|^### "` 로 목차를 뽑아 담당 구간을 특정한 뒤 그 구간을 읽는다. 최소한 §0·§6·§9 는 실제로 열어본 뒤 착수한다.
- 재발방지 게이트: §0-1 1항의 숙독 보고에 **읽은 행 범위**를 함께 적는다. 범위 없는 "숙독 완료" 보고는 미이행으로 본다.

### E-015 캐시 버전 하드코딩이 두 파일에 있는데 마감 게이트는 한 곳만 지목했다

- 발생일: 2026-08-02
- 증상: `sw.js` 를 v1.9.0 → v1.10.0 으로 올리고 §0-3 3항이 지목한 `tests/static-rules.mjs` 만 갱신해 `npm test` 5종이 전량 초록이었다. 그런데 `tests/production-smoke.mjs` 의 `assets["sw.js"]` 표식도 같은 버전을 하드코딩하고 있어, 배포 직후 `test:production` 이 반드시 실패하는 상태였다. 5종 스위트에 `test:production` 이 포함되지 않아 로컬 초록이 이 결함을 가렸다.
- 근본원인: 마감 게이트 조문이 **하드코딩 지점을 열거**하는 방식이라 지점이 하나 늘어난 뒤 조문이 따라가지 않았다. 열거형 규칙은 원본이 늘어나면 조용히 낡는다.
- 처방: §0-3 3항을 열거가 아니라 **일치 검사**로 바꿨다 — `grep -rn "ccb-v" sw.js tests/` 의 버전 문자열이 전부 같은 값이어야 한다. 같은 값을 여러 파일에 박는 규칙은 앞으로도 "목록"이 아니라 "전수 일치"로 쓴다.
- 재발방지 게이트: §0-3 3항의 grep 일치 채점. 마감 전 이 명령을 실제로 실행한 결과를 보고에 남긴다.

## §10 개선 항목 원장

### 10-0 발굴 경로와 등급 구성

발굴 경로: 원시 발견 95 → 중복제거 82 → 반증폐기 2 → 생존 80(생존 시점 등급 상 19 / 중 42 / 하 19) → 동일 뿌리 항목 병합 후 최종 등재 62(상 14 / 중 32 / 하 16). 출처는 §0 서두에 명시한 실사 산출물 2종이다.

- 순번(no)은 원장 산출물의 값을 그대로 보존한다. 재번호·재정렬을 금지한다. 폐기된 항목의 번호도 재사용하지 않는다.
- **원장 1부터 14까지(상급)가 2차 구현 대상이다.** 원장 15부터 46까지(중급)와 47부터 62까지(하급)는 백로그이며 이번 구현 대상이 아니다. 원장 63부터 67까지는 2026-07-19 실사 잔여분을 뒤늦게 편입한 하급 항목이며 같은 백로그다(§10-5).
- 상급 14건 중 9건은 사용자 승인으로 상급이 확정된 항목이다: S1(원장 12) · S2(원장 14) · S3(원장 9) · S4(원장 3) · S5(원장 4) · F1(원장 5) · F2(원장 6) · F3(원장 7) · F4(원장 8). 나머지 5건(원장 1 · 2 · 10 · 11 · 13)은 1차 실사에서 신규 확정됐다. 원장 13의 `신규(S2·F2 동반 처방)` 표기는 승인 항목이 아니라 동반 처방 계층을 뜻한다.
- 등급 강등·승격은 §2-2 판정 절차를 다시 통과해야 하며, 변경 시 이 절과 §12-2 대조표를 같은 커밋에서 갱신한다.

### 10-1 2차 구현 배치 계획

상급 14건만 대상이다. 배정 제약은 두 가지다. `app.js` 는 한 라운드에 정확히 1기만 배정한다(근거 §9 E-008). 동시 실행 기수 산정은 §9 E-001 을 따른다. `sw.js` · `data/*` · `lib/*` · `tests/*` · `index.html` · `app.css` 는 독립 병렬로 돌린다. 모든 라운드는 종료 조건으로 §0-3 마감 게이트 전항을 요구한다.

| 라운드 | 기 | 편집 파일 | 담당 원장 번호 |
| --- | --- | --- | --- |
| 1 입력·오버레이·배포·콘텐츠 | A1 | `app.js` | 1 · 4 · 9 |
| | B1 | `sw.js` · `tests/static-rules.mjs` | 12 |
| | C1 | `data/books.js` · `data/celeb-books-2025.js` · `tests/data-integrity.mjs` | 2 |
| 2 히스토리·게이트 | A2 | `app.js` | 6 · 13 · 14 · 12의 app.js 측 수신부 |
| | B2 | `tests/mobile-navigation.mjs` | 1 · 4 · 9 의 게이트 신설, §4-4 임계 단일 출처화 준비 |
| | C2 | `tests/feature-regression.mjs` | 5 · 10 · 11 의 게이트 선작성 |
| 3 저장·맞춤화·진입점 | A3 | `app.js` | 3 · 5 · 7 · 8 · 10 · 11 |
| | B3 | `index.html` · `app.css` | 7 · 8 · 10 의 마크업·스타일 |
| | C3 | 편집 없음(검증 전용) | 라운드 1부터 3까지 통합 회귀 |

배정 근거:

- A1 세 건은 오버레이 전면 재렌더라는 같은 뿌리를 공유하므로 분리하면 서로의 처방을 덮어쓴다.
- A2 네 건은 `pushState` 페이로드 단일 재설계로만 동시에 닫힌다. 개별 구현 시 서로를 덮어쓴다.
- A3 여섯 건은 전부 `state` · `sanitizeState` · 저장 경로 한 계층에 응집한다.
- 라운드 1에서 원장 4(오버레이 해제 가드)를 먼저 닫아야 라운드 2의 히스토리 재설계가 종료 팝업 오노출 경로와 얽히지 않는다.
- 라운드 2의 게이트 선작성(B2 · C2)이 라운드 3 A3 의 대규모 저장 계층 변경을 기계 채점 아래에 둔다.
- C1 은 완료 후 `tests/mobile-navigation.mjs:162-190` 을 재실행해 통과만 확인한다. 그 파일의 수정 권한은 B2 에만 있다.
- C3 은 `npm test` 전량과 375 · 768 · 1440 3폭 실측 회귀(§3-0 P-2 전처리 후)로 상급 14건의 재현 절차를 1:1 재대조한다.
- 병렬 안전성: 각 라운드 안에서 편집 파일 교차는 0이다. A2 가 `tests/mobile-navigation.mjs:310` · `:323` · `:345` 의 `history.state?.i` 단언을 통과시켜야 하므로 B2 는 그 단언을 건드리지 않는다.

### 10-2 상급 14건 — 2차 구현 대상

#### 원장 1 · 서재 검색이 한글 IME 조합을 매 글자 파괴 — 한국어 책 검색 전면 불가

- knownId: 신규 (D1 · D7 · D8)
- 증상: 서재 검색창에 한글로 제목·저자를 입력하면 조합 중이던 글자가 확정 문자로 계속 쌓여 결과가 0권이 된다. 이 앱의 제목·저자는 전부 한글이므로 서재 검색을 한국어로 쓸 수 없다. 홈 질문 검색은 정상이라 방문자는 자기 오타로 오해한다.
- 근거: `app.js:569-577` input 리스너가 매 입력마다 `renderLibrary()` 를 호출하고 `app.js:554` 가 `viewEl.innerHTML` 을 전면 교체해 조합 중인 입력 노드를 파괴한 뒤 focus·setSelectionRange 만 복원한다. `e.isComposing` 미검사이며 compositionstart·compositionend 리스너는 `app.js` 전체 0개다. CDP `Input.imeSetComposition` 실측 — '사피엔스' 입력 → 최종값 `ㅅ사사ㅍ사피사피ㅇ사피에사피엔사피엔ㅅ사피엔스사피엔스`, `.library-summary` = '전체 서재 · 0권', 카드 0장. '노인' · '논어' · '역사' 3/3 파손. 조합 이벤트 카운트 compositionstart 3회 · compositionend 0회(대조군 `#question-search` 는 1회 · 1회, 3/3 정상). `fill` 주입 시에는 1권 정상 표시.
- 처방: 1단계(게이트 무영향) — `app.js:569` 핸들러 진입부에 `if (e.isComposing) { libQuery = input.value; return; }` 가드를 두고 같은 노드에 compositionend 리스너를 달아 1회만 `renderLibrary()`. 2단계(근본, 별도 커밋) — 목록만 `#lib-list` 컨테이너에 그리는 `renderLibraryList()` 로 분리해 입력 노드를 재생성하지 않는다. 이때 `app.js:571-576` 의 selectionStart 저장·복원 코드를 삭제한다.
- 영향 파일: `app.js` · `tests/mobile-navigation.mjs` · `tests/feature-regression.mjs`
- 게이트 리스크: 1단계는 무영향. 2단계는 `.view > .card` 선택자를 쓰는 `tests/feature-regression.mjs:76` · `:79` · `:81` · `:84` 와 `tests/mobile-navigation.mjs:213` · `:334` 를 깬다. 같은 커밋 동반 갱신이 필수다.

#### 원장 2 · 자동 생성 질문이 어절 중간에서 잘린 비문

- knownId: 신규 (D5 · D6)
- 증상: 첫 화면 오늘의 질문이 단어 한복판에서 잘린 인용문으로 뜬다. 방문자는 원전 인용으로 읽지만 문장이 성립하지 않아 답을 적을 수 없고, 수집하면 기록 탭에도 잘린 그대로 저장된다. 질문으로 입장한다는 제품 약속이 첫 문장에서 깨진다.
- 근거: `data/books.js:401-405` `derivedQuestion` 이 `clean.slice(0, Math.max(8, 44 - suffix.length - 2))` 로 어절 경계를 무시하고 26자·23자에서 절단하며, 같은 로직이 `data/celeb-books-2025.js:169-174` `principleQuestion` 에 복제되어 있다. `data/books.js:407-419` `ensureQuestionDepth` 가 그 결과를 질문 3개 미만 책에 자동 삽입한다. 전수 스캔 2종: 어절 중간 절단 172/590(29.2%), 인용부가 principle 접두이면서 종결어미로 끝나지 않는 항목 128/590(21.7%). 템플릿 질문 276건 중 원문보다 짧게 잘린 것 223건. 상세 시트에 비문 1건 이상 뜨는 책 133/175권(76.0%).
- 처방: 두 절단 지점을 마지막 공백 기준으로 바꾸고 말줄임표를 부착한다. 판정 기준을 '인용부가 원문 접두이면 반드시 말줄임표로 끝난다'로 통일해 공백에서 끊겨 조사만 남는 유형까지 함께 걸러낸다. 경계가 없거나 8자 미만이면 비인용형 대체 문구를 쓰되 책 id 고유 요소를 포함해 문구 중복을 회피한다.
- 영향 파일: `data/books.js` · `data/celeb-books-2025.js` · `tests/data-integrity.mjs`
- 게이트 리스크: `tests/data-integrity.mjs:29`(44자 이하) · `:85`(질문 문구 중복 금지)는 350건 재생성 시뮬레이션에서 통과했으나 실행 검증이 필요하다. `tests/mobile-navigation.mjs:162-190` 은 텍스트 길이 변경 시 반드시 재실행한다.

#### 원장 3 · save() 가 state 를 통째로 덮어쓰고 storage 미청취

- knownId: S4 (사용자 승인 확정) (D2 · D4 · D8)
- 증상: 앱을 열기만 하고 아무것도 누르지 않아도 기기에 데이터가 기록된다. 두 탭을 열어 두면 나중에 조작한 탭이 먼저 탭의 기록을 통째로 덮어쓰고, 덮어쓴 탭 화면에는 사라진 항목이 그대로 남아 소실이 은폐된다.
- 근거: `app.js:125-133` `save()` 가 메모리 state 전체를 write 하고 `storage` 이벤트 청취가 앱 전체 0건이다. `app.js:195` `drawQuestion` 내부 `save()` 가 `app.js:199` 모듈 평가 시점에 실행되어, 입력 0회 상태에서 키 11개·questionDeck 589건·11,018바이트가 58ms에서 150ms 사이에 기록된다. 다중 탭 실측 3회차 — (a) A탭 질문 5개 수집 + 답변 1,445자 후 B탭 '다른 질문' 1회 클릭 → 저장 상태 질문 0개·0자, A탭 화면은 답변 칸 5개 유지. (b) A탭 읽음 3권·질문 3개 → 최종 저장에 A의 id 0건. (c) 탭1 수집 1건 → 탭2 수집 2건 → 탭1 셔플 1회 → 1건으로 되돌아감.
- 처방: (1) `app.js:185-197` 의 로드 시점 무조건 `save()` 를 제거하고 히어로 질문 확정을 [다른 질문] 또는 날짜 변경 시로 한정하되, 재방문 전 최소 1회(pagehide 포함) 확정을 보장한다. (2) `save()` 를 읽기·병합·쓰기로 전환하고 `storage` 리스너로 타 탭 변경을 state 에 반영한 뒤 render 한다(DI-5). (3) 병합 후에도 version 은 현행 값을 유지한다.
- 영향 파일: `app.js` · `tests/mobile-navigation.mjs` · `tests/feature-regression.mjs`
- 게이트 리스크: `tests/mobile-navigation.mjs:144-149` 가 로드 시 questionDeck·lastHeroQuestionId 확정에 의존하므로 (1)은 pagehide 확정과 동시에 적용해야 통과한다. `tests/feature-regression.mjs:179` 의 version 단언을 유지해야 한다(§12-3 의 version 필드 정리와 상충하므로 순서를 정한 뒤 진행한다).

#### 원장 4 · 오버레이 닫힘 경합 — 더블탭이 다른 여정을 시작하거나 종료 팝업을 띄우고 중복 기록을 남긴다

- knownId: S5 (사용자 승인 확정) (D2 · D3 · D4)
- 증상: 여정 완료 저장을 사람 속도로 두 번 누르면 시트가 닫히는 사이 배경이 활성화되어 같은 좌표의 여정 목록 카드가 눌린다. 저장만 했는데 고르지 않은 여정이 시작·기록되고, 진행 중 여정에는 그만두기 버튼이 없어 되돌릴 수 없다. 프로필 저장·이름 지우기 더블클릭은 종료 팝업 오노출 또는 서재 탭 이탈로, 동일 태스크 이중 호출은 journeysDone 중복 기록으로 나타난다.
- 근거: `app.js:875-880` `finishJourney` · `app.js:845-852` `saveProfile` · `app.js:853-857` `clearProfile` 이 모두 재진입 가드 없이 비동기 `history.back()` 을 호출한다. 실측 popstate 지연 16ms · rAF 39ms 뒤 배경 inert 가 해제된다(`app.js:232-240`). 여정 완료 더블탭 — 좌표 (195,708) 두 번째 클릭이 `button[data-start-journey="j-economy"]` 에 적중해 `state.journey` 가 교체됨(80 · 150 · 250ms 전부 재현). 취소 경로 부재 — `app.js:878` 이 `state.journey = null` 의 유일한 대입부이고 `app.js:706` 이 나머지를 잠금(실측 enabled 1/6). 프로필 저장 더블탭 — 375px 저장 버튼 박스 y=744에서 788, `.tabbar` top=753 으로 35px 겹침, 시트 닫힘 후 `elementFromPoint(188,766)` = `button.tab[data-tab=library]`. 1440x900 dblclick 3/3 종료 팝업(`app.js:250-258` 이 `i<0` 을 센티널로 오판). 동일 태스크 click 3회 → journeysDone 3건.
- 처방: `app.js:242-247` · `:852` · `:856` · `:880` · `:919` 를 감싸는 단일 `dismissOverlay()` 를 만들고 재진입 플래그를 popstate 도착 시 해제한다. 해제 지점은 `app.js:251` 직후이며 조기 return 경로(`:253-259`)에 두면 플래그가 고착된다(N-6). 병행 처방 — 핸들러 진입 즉시 `t.disabled = true`, 오버레이 해제 직후 약 150ms `.tabbar` `pointer-events: none`(inert 금지, N-7), `app.js:875` 진입부에 journeysDone 중복 가드(N-8), `renderJourneyDetail`(`app.js:759-772`)에 여정 그만두기 추가. `.sheet` 하단 패딩 확대는 보조 처방으로만 쓴다.
- 영향 파일: `app.js` · `app.css` · `tests/mobile-navigation.mjs`
- 게이트 리스크: 낮음. `tests/mobile-navigation.mjs:247-276` 프로필 시나리오는 단일 클릭이라 통과하며, 가드 해제 누락 시 `:265` · `:272` 가 먼저 깨져 회귀를 잡는다. `:137-142` · `:389-394` 의 44px 감사는 시트 미개방 화면 대상이라 무영향이다. `tests/feature-regression.mjs` 의 여정 순서 단언과 중복 가드는 무충돌이다.

#### 원장 5 · 관심 분야·서재 필터 선택이 저장되지 않아 재방문마다 초기화

- knownId: F1 (사용자 승인 확정) (D2 · D5 · D6)
- 증상: 계보에서 과학을 고르고 서재를 뿌리로 좁혀 둔 방문자가 다음에 앱을 열면 계보는 철학, 서재는 전체로 되돌아가 있다. 매 방문마다 같은 설정을 다시 해야 한다. §2-1 C5-1 위반이다.
- 근거: `app.js:412` `let sessionDomain = DOMAINS[0]`, `app.js:413` `let libQuery = "", libDomain = "전체", libTier = "전체"` 가 전부 모듈 변수이며 `sanitizeState` 반환 필드(`app.js:98-108`)에 관심 분야 슬롯이 없다. 실측 — 계보 과학 선택 직후 저장 키 11개 중 domain·filter·tab 계열 0개, 재방문 시 선택은 철학. 서재 뿌리 적용 시 20권 → 재방문 175권, 계단 칩 `aria-pressed` 가 전체로 복귀. 기본값 철학은 데이터 분포와도 어긋난다(분야 편중 실측은 원장 59). 렌더는 `app.js:533` · `:537` 이 sessionDomain 만 참조한다.
- 처방: `sanitizeState` 반환에 `prefs: { lineageDomain, libDomain, libTier }` 를 추가하고 DOMAINS 및 전체·뿌리·줄기·가지 화이트리스트로 검증한다. `app.js:898-908` 칩 핸들러에서 save 하고, 로드 시 `app.js:412-413` 초기값을 prefs 에서 복원한다. 검색어는 복원 대상에서 제외한다(C5-2). 기본값 우선순위는 prefs → 읽음 최다 분야 추론 → `DOMAINS[0]` 로 고정해 원장 32의 개인화 처방과 충돌하지 않게 한다.
- 영향 파일: `app.js` · `tests/feature-regression.mjs`
- 게이트 리스크: 없음. `tests/feature-regression.mjs` 의 정화 단언은 필드 단위 비교라 state 필드 추가에 영향받지 않는다.

#### 원장 6 · 해시 딥링크·새로고침이 무시되고 요청 위치가 #question 으로 덮어써진다

- knownId: F2 (사용자 승인 확정) (D1 · D2 · D3 · D8)
- 증상: 공유받은 `/#library` 링크로 들어오거나 서재를 보다가 새로고침하면 항상 홈으로 시작하고 요청한 위치가 소실된다. 보던 자리로 돌아갈 방법이 없다. §2-1 C1-2 위반이다.
- 근거: `app.js:205-206` 이 `history.replaceState({ i: -1 }, "")` 직후 `history.pushState({ i: 0 }, "", HASH.question)` 로 진입 해시를 무조건 덮어쓴다. `app.js:203` 의 스택 초기값이 고정이고 `location.hash` 를 읽는 코드와 hashchange 리스너가 0건이다. 실측 — `/#library` 진입 시 표시 탭은 홈, 최종 URL 은 `#question`, 이어서 뒤로가기 1회에 종료 팝업. `/#record` 도 동일하며 768 · 1440 에서 재현된다.
- 처방: 부팅 시 `location.hash` 를 `HASH` 역매핑으로 조회해 스택 초기값과 첫 pushState 대상 탭을 결정한다. 미지 해시는 홈으로 폴백하고 요청 해시를 다른 값으로 재기입하지 않는다. 센티널 replaceState 는 유지한다. 오버레이는 하위 탭과 해시가 같아 주소로 구분되지 않으므로 복원 범위를 탭 단위로 한정한다(§6-5).
- 영향 파일: `app.js` · `tests/mobile-navigation.mjs`
- 게이트 리스크: 없음(확인 완료). `tests/mobile-navigation.mjs:129` · `:235` · `:309` · `:356` 은 해시 없는 진입이라 결과가 유지되고, `tests/feature-regression.mjs:133` 은 `#record` 로 reload 하지만 직후 `:134` 의 record 탭 클릭이 `app.js:860` 동일 탭 no-op 로 흡수되어 `:135` 단언이 통과한다.

#### 원장 7 · 첫 방문자에게 내 서재 등록·기록 보관을 알리는 단서가 상단 버튼 하나뿐

- knownId: F3 (사용자 승인 확정) (D2)
- 증상: 처음 온 방문자는 앱이 이름을 기억한다는 사실도, 기록이 이 기기에 쌓인다는 사실도 알 길이 없다. 상단 구석의 작은 버튼을 우연히 눌러야 알게 된다. §2-1 C2-2 위반이다.
- 근거: 첫 화면 전체 innerText 스캔에서 '이름' · '별명' · '등록' · '저장' · '설정' · '환영' 각 0회, '내 서재' 1회(상단 버튼 라벨)다. `#profile-btn` 실측 73.6x44px, 13px, 화면폭 대비 19.6%(375) · 9.6%(768) · 5.1%(1440)로 넓은 화면일수록 존재감이 줄어든다. `app.js` 전역에 first-run 분기가 없다. 보관 안내 문구는 시트를 연 뒤에야 노출된다(`app.js:788`).
- 처방: 기록 0건 첫 방문자에게만 홈 히어로 카드 직후 1회성 안내 카드를 렌더하고 `state.onboardingDismissed` 로 해제 상태를 저장한다(`sanitizeState` 화이트리스트 등재). 삽입 위치는 히어로 직후로 고정해 홈 정보 순서 표식을 깨지 않는다(INV-1).
- 영향 파일: `app.js`
- 게이트 리스크: 중간. `tests/static-rules.mjs:31-34` 의 홈 정보 순서 표식을 유지해야 하고, 문구가 `:66`(인증 어휘) · `:64`(이모지) · `:63`(그라데이션) 또는 `tests/mobile-navigation.mjs:250` 을 건드리면 즉시 실패한다. 새 버튼은 44px 게이트를 준수한다.

#### 원장 8 · 맞춤설정 화면·기록 초기화 수단 전무

- knownId: F4 (사용자 승인 확정) (D2 · D4 · D6)
- 증상: 공용 기기에서 앱을 쓴 방문자가 기록을 지우려 해도 앱 안에 삭제 수단이 없다. 이름 지우기를 눌러도 이름만 사라지고 기록 탭에는 쓴 답변이 그대로 남아 다음 사람에게 보인다. §2-1 C4-1 과 C6-1 부터 C6-4 까지 전부 위반이다.
- 근거: 4탭과 프로필 오버레이 전수 버튼 스캔에서 초기화·삭제·설정 계열 0개다(프로필 시트 버튼은 닫기·저장뿐). `app.js:853-857` clear-profile 은 `state.profile = null` 만 수행하며, 실측으로 이름 등록 + 질문 1건 수집 + 답변 입력 후 이름 지우기를 실행하면 profile 은 null 이지만 `questions[0].myAnswer` 가 유지되고 기록 탭 textarea 값으로 노출된다. `index.html:29-31` 상단바에는 버튼 2개뿐이고 설정 오버레이는 `app.js:812-817` 의 오버레이 5종에 없다. 내보내기·백업·다운로드 문구도 전 화면 0건이다.
- 처방: 내 서재 설정 오버레이를 신설해 관심 분야·테마·기록 초기화·파일 내보내기와 불러오기를 한곳에 담는다. 삭제 실행은 `localStorage.removeItem` 후 in-memory state 를 `sanitizeState()` 로 재초기화해야 한다 — `app.js:118` 의 state 는 모듈 상수라 removeItem 만으로는 다음 save 에서 되살아난다. 불러오기는 `sanitizeState`(`app.js:64-109`)를 반드시 통과시키고 읽음·읽는 중 교집합 제거 규칙(`app.js:67`)을 유지한다(DI-7). 읽은 책·수집 질문 수치는 기록 탭이 원천이므로 여기서 재표시하지 않고 기록 탭 이동 버튼 1개만 둔다(R6).
- 영향 파일: `app.js` · `index.html`
- 게이트 리스크: 중간. 문구는 `tests/static-rules.mjs:66` 금지어를 피한다. 상단 테마 토글을 설정으로 이관하면 `tests/mobile-navigation.mjs:192-202` 와 `tests/static-rules.mjs:69` 가 동반 갱신 대상이다. `Blob` 과 `a[download]` 은 `:65` 외부 요청 정규식에 걸리지 않는다.

#### 원장 9 · 여정 완료 답 초안이 체크박스 조작 한 번에 사라진다

- knownId: S3 (사용자 승인 확정) (D4 · D7 · D8)
- 증상: 여정 마지막 책까지 체크한 뒤 완료 답을 길게 쓰다가 체크를 하나 고치면 쓰던 답이 통째로 사라진다.
- 근거: `app.js:752` `<textarea id="j-answer" …>` 의 DOM 속성이 id 와 placeholder 뿐이라 `app.js:956-968` 입력 위임 저장 대상에 걸리지 않고 초기값도 state 에서 채우지 않는다. `app.js:936-954` change 위임이 마지막에 render 를 호출하고 render 는 `app.js:816` 을 거쳐 `overlayRoot.innerHTML` 을 재생성한다(`app.js:759-773`) — 체크박스 4회 클릭 모두 포커스가 `div.sheet` 로 이동해(4/4) 노드 교체를 확인했다. 실측 25자 초안 입력 후 마지막 체크 해제·재체크 시 textarea 값은 공백, 저장된 journeysDone 0건이다. 부수 결함으로 `label[for="j-answer"]` 가 0건이어서 접근 이름이 placeholder 단일 출처다.
- 처방: 완료 답을 `state.journeyDraft` 로 승격하고 textarea 에 `data-answer-*` 를 부여해 `app.js:956-968` 입력 저장 경로에 연결하며, `renderJourneyDetail` 이 그 값을 복원한다(§5-1 과 같은 상한을 DI-3 상수로 공유). 체크박스 change 는 오버레이 전면 재렌더 대신 체크 상태·진행 문구(`app.js:765`)·완료 카드 표시 여부만 부분 갱신한다. sr-only label 을 함께 추가한다.
- 영향 파일: `app.js` · `tests/feature-regression.mjs`
- 게이트 리스크: 없음. 기존 답변 저장 단언은 `data-answer-q` 경로이고 `tests/static-rules.mjs:73` 은 답변 상한 방어만 요구하므로 새 draft 필드에 같은 상한을 적용하면 충돌이 없다. `tests/feature-regression.mjs:142` · `:148` 의 `[data-jcheck]` disabled 배열은 부분 갱신 후에도 유지한다.

#### 원장 10 · 저장 실패를 무시하고 성공한 것처럼 표시 — 시각 사용자에게 통보 0

- knownId: 신규 (D4 · D8)
- 증상: 저장이 실패해도 수집 버튼은 수집됨으로 바뀌고 읽음 배지가 켜진다. 화면 어디에도 실패 표시가 없어 방문자는 계속 글을 쓰다가 앱을 다시 열었을 때 전부 사라진 것을 발견한다. DI-4 위반이다.
- 근거: `app.js:125-133` `save()` 가 성공·실패 bool 을 반환하지만 호출부 전원이 무시한다(`app.js:163` · `:195` · `:841` · `:851` · `:856` · `:866` · `:873` · `:879` · `:885` · `:952`). 실패 통보는 `announce()`(`app.js:121-124`) → `#app-status`(`index.html:57`) 뿐이며 `app.css:436-439` 로 1x1px clip 이다. 헤드룸 0 실측 — 기록 탭 5,400자 입력 → 저장 0자, 화면 실패 문구 노드 0개, 직후 읽음 라벨과 배지는 갱신됨, 재로드 후 답변 0자·읽음 0건. QuotaExceededError 주입 회차에서도 버튼은 수집됨 + disabled 로 전환된다. 도달 근거는 §5-2 의 용량 대비다.
- 처방: `save()` 실패 시 (1) 방금 변경한 state 를 롤백하고 (2) 보이는 오류 배너(`role="alert"`, `app.css:441` 기존 스타일 재사용, 대비 7.7:1 확인)를 렌더한 뒤 (3) 낙관적 UI 반영을 중단한다. 반환값 검사는 `app.js:163` · `:851` · `:856` · `:873` · `:879` · `:885` · `:952` 에 우선 적용한다. 문구는 저장 공간 부족 단정 대신 저장 거부 문맥까지 포괄하는 표현을 쓰고 `loadState` 실패(`app.js:112-116`)도 같은 배너로 알린다. `announce()` 호출은 제거하지 않고 유지한다.
- 영향 파일: `app.js` · `app.css` · `index.html` · `tests/feature-regression.mjs`
- 게이트 리스크: 있음(관리 가능). `tests/feature-regression.mjs:204` 가 저장 실패 시 `#app-status` 에 특정 문구를 요구하므로 announce 를 배너로 대체하면 깨진다. 배너는 추가만 한다. 배너 문구에 `tests/static-rules.mjs:63-66` 금지 요소를 넣지 않는다.

#### 원장 11 · 책 목록이 바뀌면 방문자 기록이 로드 270ms 안에 무통보 삭제된다

- knownId: 신규 (D4)
- 증상: 앱 업데이트로 책 목록이 교체·정리되면 그 책에 달아 둔 읽음·수집 질문·직접 쓴 답·여정 진행이 아무 안내 없이 사라진다. 되돌릴 방법도, 사라졌다는 사실을 알 방법도 없다. DI-1 위반이다.
- 근거: `app.js:60-62` 가 현재 카탈로그에 없는 id 를 전부 폐기하고, `app.js:69-76` 은 `bookId#배열인덱스` 통과분만 생존시키며, `app.js:82-85` 는 여정 진행을 첫 결번에서 자르고, `app.js:185-197` 이 로드 직후 save 로 정리 결과를 즉시 영속화한다. 실측 — 읽음 5→2, 읽는 중 1→0, 질문 3→1(답변 253자 소실), journeysDone 2→1, 안내 0건, 로드 후 270ms 내 확정. 여정 중간 1권 소실 시 doneBookIds 3→1. 트리거 근거는 `data/books.js:422-432` 의 1,000권 교체 주석과 `data/celeb-books-2025.js:197` 의 id 유지 주석이다. 질문 배열 마지막 항목은 principle 파생 문장이라 원문 한 글자만 고쳐도 같은 id 가 다른 질문을 가리킨다(DI-2).
- 처방: 카탈로그 정리 결과를 즉시 확정하지 않고 원본 항목을 `state.orphans` 로 격리 보존해 함께 저장한다. 로드 시 save 자체를 없애지는 않는다(questionDeck·lastHeroQuestionId 영속화가 걸려 있다). 질문 id 를 배열 인덱스 파생에서 안정 qid 로 승격하고, 여정 진행은 첫 결번에서 절단하지 않고 결번만 건너뛴다. 격리 항목이 있으면 기록 탭에 사유를 1회 고지한다.
- 영향 파일: `app.js` · `data/books.js` · `data/celeb-books-2025.js` · `tests/feature-regression.mjs`
- 게이트 리스크: 높음. `tests/feature-regression.mjs:178-190` 이 삭제 방식의 정화 결과를 직접 단언하므로 격리 전환 시 같은 커밋 갱신이 필수다. 로드 시 save 를 지연시키면 `tests/mobile-navigation.mjs:149` 통과가 확률에 의존하게 된다.

#### 원장 12 · 배포 시 열린 탭의 history.state 가 소실되어 종료 팝업 오노출·URL 데스싱크

- knownId: S1 (사용자 승인 확정) (D1 · D8)
- 증상: 새 버전이 배포되면 읽던 탭에서 화면은 그대로인데 주소만 다른 해시로 바뀌고 종료 팝업이 뜬다. 회차에 따라 수십 초 응답 불능이나 앱 재부팅으로도 나타나 방문자는 앱이 죽은 것으로 판단한다. §6-3 위반이다.
- 근거: `sw.js:31` 이 activate 의 waitUntil 안에서 `client.navigate(client.url)` 로 열린 클라이언트를 강제 이동시킨다. 실측 — 서재 탭 + 검색어 상태(`history.state.i=1`)에서 캐시 버전을 바꾼 `sw.js` 배포 후 `registration.update()` → `history.state?.i === undefined`, 종료 팝업 표시, 표시 탭은 서재 유지, URL 은 `#question`. navigate 가 만든 state 없는 항목이 `app.js:252` 에서 센티널로 오판되어 팝업을 호출한다. 다른 회차에서 3초 타임아웃 7연속(24초 이상), 305초 후 컨텍스트 복구·메인프레임 내비게이션 3회, URL 만 어긋나는 사례를 관측했다. 신 워커 activating 고착과 구·신 캐시 병존도 관측됐다.
- 처방: `sw.js:29-31` 의 activate 내 `client.navigate` 를 제거하고 postMessage 로 갱신 사실만 통지한 뒤, `app.js` 가 안전한 시점(오버레이·입력 없음)에 사용자 확인 후 reload 한다. 프리즈는 재현 조건이 특정되지 않았으므로 처방 근거로 삼지 않고, 정정된 증상은 갱신 직후 state 소실에 따른 팝업 오노출과 URL 데스싱크로 기록한다. 원장 13과 같은 계층이므로 함께 닫는다.
- 영향 파일: `sw.js` · `app.js` · `tests/static-rules.mjs`
- 게이트 리스크: 높음. `tests/static-rules.mjs:59` 가 `client.navigate` 존재를 강제하므로 같은 커밋에서 규칙을 교체해야 한다. `:57` 캐시 버전은 §0-3 3항에 따라 동시 갱신한다. `tests/mobile-navigation.mjs:134` 는 SW 제어 상태만 보므로 무영향이다.

#### 원장 13 · 상태 없는 히스토리 항목을 종료 센티널로 오판

- knownId: 신규(S2 · F2 동반 처방) (D8)
- 증상: 주소 재입력·북마크·공유 링크·세션 복원·서비스워커 갱신 등 같은 문서의 해시가 바뀌는 모든 상황에서 종료 의사를 밝힌 적이 없는데 종료 팝업이 뜬다. 팝업 중 화면은 이전 탭 그대로인데 URL 만 어긋나고, 앞으로가기하면 팝업이 다시 뜨면서 상단바가 잠긴다. N-1 · N-3 위반이다.
- 근거: `app.js:252` `const i = e.state && typeof e.state.i === "number" ? e.state.i : -1;` 가 state 가 null 인 미지 항목을 센티널과 같은 값으로 접는다. `app.js:205` 의 실제 센티널도 같은 값이라 구분할 수 없다. 실측 — 계보 탭(i=1)에서 `location.hash = "#library"` → state null, 팝업 표시, 표시 탭 계보 유지, URL `#library`. 뒤로 → 비표시, 앞으로 → 표시 + `topbar.inert = true`. SW 갱신 경로에서도 동일하다.
- 처방: 센티널을 `{ sentinel: true }` 같은 고유 값으로 식별하고, 미지 state 는 센티널로 처리하지 않고 현재 뷰 기준으로 재구성한 뒤 replaceState 로 인덱스를 재기입한다. `app.js:377` 의 닫힘 표식도 미지 state 로 분류되지 않도록 `app.js:251` 가드와 함께 검토한다. 원장 6 · 12 · 14와 동일 계층이므로 pushState 페이로드를 한 번에 재설계해 네 항목을 같은 커밋에서 닫는다.
- 영향 파일: `app.js` · `tests/mobile-navigation.mjs`
- 게이트 리스크: 중간. `tests/mobile-navigation.mjs:232-235` · `:311-312` · `:324-325` · `:346-347` 의 뒤로가기 팝업 단언과 `:310` · `:323` · `:345` 의 인덱스 단언이 모두 통과해야 한다. 센티널은 replaceState 로 만들어진 최초 항목이므로 값 식별 전환 시 `:355` 도 확인 대상이다.

#### 원장 14 · 앞으로가기 데스싱크 — URL만 진행하고 화면은 고정

- knownId: S2 (사용자 승인 확정) (D3)
- 증상: 뒤로가기로 되돌아간 뒤 앞으로가기를 누르면 주소 해시만 진행하고 화면은 못박힌다. 이어서 뒤로가기를 눌러도 두 번 연속 화면이 변하지 않아 버튼이 죽은 것으로 인식한다. N-2 위반이다.
- 근거: `app.js:245` 가 인덱스만 저장하고 `app.js:266` `stack = stack.slice(0, i + 1)` 가 앞쪽 항목을 파괴하므로 forward 복원이 불가능하다. 실측 시퀀스 — i=3 기록 → 뒤로 i=2 서재 → 뒤로 i=1 계보 → 앞으로 i=2(URL 서재 / 렌더 계보) → 앞으로 i=3(URL 기록 / 렌더 계보) → 앞으로 무변화 → 뒤로 3회 무변화 → 뒤로 i=0 홈 → 뒤로 종료 팝업. URL 4회 변경 동안 화면 0회 변경이다.
- 처방: pushState·replaceState 페이로드에 `{ i, view: { tab, overlay } }` 를 실어 popstate 에서 뷰를 복원하고 slice 를 제거한다(스택 보존 + 포인터 이동 방식도 유효). 뷰를 직렬화하면 오버레이도 복원되므로 `app.js:243` 의 복귀 포커스 기록과 `app.js:269` 복원 조건의 정합을 맞춘다.
- 영향 파일: `app.js` · `tests/mobile-navigation.mjs`
- 게이트 리스크: 낮음. 인덱스를 단언하는 `tests/mobile-navigation.mjs:310` · `:323` · `:345` 는 `i` 필드를 유지하면 통과한다. 원장 13과 같은 커밋에서 진행해 서로의 페이로드 설계를 덮어쓰지 않게 한다.

### 10-3 중급 32건 — 백로그

이번 구현 대상이 아니다. 상급 항목의 처방에 흡수되는 항목은 처방 요약에 흡수 대상을 명시했으며 별건으로 세지 않는다.

| 번호 | 제목 | 근거 요약 | 처방 요약 |
| --- | --- | --- | --- |
| 15 | 2글자 제목·저자 정확 입력에도 검색 0건 | `lib/search.js:80` · `:120` 으로 2글자 identity 9점, `:129` 임계 10점에서 탈락. 2글자 제목·저자 23종 중 17종(73.9%) 0건 | 임계는 유지하고 제목·저자 완전일치에 +20 보너스. 조기 반환 경로라 기존 벤치마크 무영향 |
| 16 | 서재 검색과 홈 검색의 정규화 규칙 불일치 | `app.js:550` 무정규화 부분문자열. '총, 균, 쇠' 1건 대 '총균쇠' 0건, 'SayNo' 1건 대 'sayno' 0건 | `normalizeSearchText`(`lib/search.js:22-29`)로 양쪽 통일. 모듈 로드 시 1회 인덱싱 |
| 17 | 1글자 주제어 UI 차단·공백 제출 무반응 | `app.js:440` minlength 2 대 엔진은 1자 앵커 허용(`tests/search-quality.mjs:68-71`). 공백 제출 시 안내 0건 | minlength 를 1로 낮추고 공백 전용 입력 안내 분기 추가. UI 도달성은 DOM 어서션으로 보증 |
| 18 | 주제어 사전 60개 고정·조사 커버리지 누락 | `lib/search.js:2-11` 9그룹 60어휘, `:19` JOSA 에 에·도·만·이나·라도 누락. 대표 질의 50건 중 16건(32.0%) 부당 0건 | JOSA 확장 + 생활·현대 주제 그룹 추가 + 빈 결과에 등록 주제어 칩. 그룹 확장 후 검색 게이트 전량 재실행 |
| 19 | 탭 전환 시 이전 화면 스크롤 잔존 | `app.js:857-861` 스크롤 초기화 없음. 3뷰포트 전부 3,000px 이상 잔존 | 탭이 바뀌는 pushView 경로만 최상단 복귀(N-9) |
| 20 | 전면 재렌더로 액션마다 포커스·스크롤 소실 | `app.js:887` · `:890` · `:900` · `:904` · `:908` · `:911` · `:914` 의 `render()` 가 innerHTML 교체. 액션 후 activeElement 7/7 이 BODY | render 래퍼에 포커스 기억·재조회 복원. 오버레이는 최초 진입 1회만 시트 포커스 |
| 21 | 상태 변화가 스크린리더에 낭독되지 않음 | `announce()` 호출부가 `app.js:130` 1곳. 10개 액션 낭독 0건. 검색 안내가 템플릿 내부라 노드 교체 | 라이브 리전을 영속 DOM 으로 이관하고 주요 액션에 announce 1문장. 탭 전환은 `#view` 포커스 |
| 22 | 부팅 구간 본문 공백·탭 입력 소실 | 모듈 3단 체인 평가 후 render. 공백 구간 4G 1,065ms · Fast-3G 2,896ms, 부팅 중 탭 클릭 소실 | `#view` 정적 자리표시 + aria-busy, 탭바 입력 잠금 후 render 직후 해제. 동적 import 분할은 제외 |
| 23 | 남색 테마 재방문 시 밝은 화면 섬광 | `applyTheme`(`app.js:141-146`)이 모듈 평가 후 실행. CPU4x 에서 453ms 이상 은회 노출 | head 인라인 스크립트로 첫 페인트 전에 테마 속성과 theme-color 설정 |
| 24 | 히어로 질문 최소 단계가 본문보다 작아 위계 역전 | `app.css:148` · `app.js:489`. 압축 최하단이 본문·카드 제목·스트립 수치보다 작다(적용 건수·비율 §4-4) | 4단을 3단으로 축소하고 하한 고정(§4-4). `tests/mobile-navigation.mjs:180` 동시 갱신 |
| 25 | 200% 확대 시 히어로 질문 잘림 | `app.css:130-148` 고정 높이 + 폭 미반영. 188px 폭에서 16/16 잘림·최대 74px 손실·가로 스크롤 발생 | 고정 높이를 최소 높이로 바꾸고 폰트 단계에 뷰포트 폭 반영. 게이트 폭에 320px · 188px 추가 |
| 26 | 긴 이름 저장 시 상단바가 68px 에서 164px 로 팽창 | `app.js:820` 표시 길이 제한 없음, `app.css:80-90` 말줄임 없음, `:79` min-width 없음 | `.profile-btn` 말줄임 + `.brand-sub` min-width 0. 게이트는 상단바 높이 단언 1건만 신설 |
| 27 | 이름 입력창 Enter 무동작 | `app.js:786-791` 은 form 밖 단독 input 이고 keydown 리스너 0건 | form 으로 감싸고 submit 위임에 id 분기. 저장 로직을 함수로 추출해 이중 실행 방지 |
| 28 | 이름 오류 메시지가 낭독되지 않고 해제되지 않음 | `app.js:793` 텍스트 고정 + `:849` 가시성 토글만. 해제 코드 0건 | 문구를 textContent 로 주입하고 input 리스너에서 해제. `aria-invalid` · `aria-describedby` 연결 |
| 29 | 서재에서 하단 탭바까지 Tab 99회 | DOM 순서 + 건너뛰기 링크 0건. 서재 102스톱 중 탭바가 99번째 | body 최상단 sr-only 건너뛰기 링크(포커스 시 가시화). DOM 순서 변경은 채택하지 않음 |
| 30 | 여정 체크 해제 후 강제 읽음 잔존 | `app.js:947` 이 읽음을 강제하는데 `:948-951` 해제 분기에 원복이 없다. doneBookIds 0 대 읽음 3 유지 | 읽음 출처를 기록해 해제 시 그 책만 원복. 진행 초기화 로직은 게이트 강제분이므로 불변 |
| 31 | 내보내기·백업 수단 0 | 전 화면에서 내보내기·백업·다운로드 문구 0건. 원장 10 · 11 · 53의 복구 경로를 0으로 만든다 | 원장 8 설정 오버레이에 흡수. 불러오기는 정화 통과 필수 |
| 32 | 저장 이력이 개인화에 소비되지 않음 | 한 분야 전량 읽음 상태 20회 재방문에서 해당 분야 히어로 노출 10.0% 대 무가중 기대치 13.7%. 서재 정렬 개인화 인자 0 | `drawQuestion` 에 분야 가중 상한 2배 적용. 순환·직전 회피 로직 보존. 계보 기본 분야는 원장 5 에서 처리 |
| 33 | 나의 기록 4칸 중 2칸이 랜덤 책 통계 | `app.js:507` 그룹 라벨 안에 `:509` · `:510` 히어로 책 통계. 수집 0건에서 분모가 요동 | 값 계산은 불변으로 두고 라벨을 회전 사양과 문맥에 맞게 분리 |
| 34 | 뿌리까지 백링크가 분야를 전달하지 않음 | `app.js:510` 페이로드 없음. 역사 책에서 계보 철학으로 착지 | 분야 데이터 속성 추가 후 탭 분기에서 대입. `tests/static-rules.mjs:91` 정규식 창 유지. 원장 5와 분리 커밋 |
| 35 | 오버레이 주 CTA 가 오버레이를 스택에 남긴다 | `app.js:915-917` 이 pushView 만 호출해 뒤로가기에 닫은 모달이 부활(i=4 중첩) | 오버레이 항목을 replaceState 로 치환. `history.go(-d)` 후 pushView 방식은 채택하지 않음 |
| 36 | 검색·필터 변경이 히스토리에 없어 뒤로가기가 앱을 이탈시킨다 | `app.js:923-934` · `:898-911` 에 pushState 없음. 검색 제출 후 뒤로 1회에 종료 팝업 | 히스토리 확장보다 명시 컨트롤 우선 — 검색 지우기·필터 초기화 노출 |
| 37 | 최근 질문 카드 착지점이 화면 밖 2,028px | `app.js:517-521` 이 대상 식별자를 넘기지 않고 `:590-600` 은 수집순 렌더 | 대상 질문 식별자를 실어 해당 항목으로 스크롤 후 정적 강조. 정렬 역전은 채택하지 않음 |
| 38 | 닫힘 화면 이후 복귀 경로 0 | `app.js:372-386` body 교체 + `:250-251` 이후 이동 무시. 포커스 가능 요소 0개 | 닫힘 화면에 다시 열기 버튼 추가(§6-7, `app.css:433` 기존 스타일 사용). 원장 57과 동일 처방 |
| 39 | 종료 팝업 닫기가 오버레이 배경 잠금까지 해제 | `app.js:279-285` 무조건 해제, `:294-300` 복원 없음. 시트 열린 상태에서 ESC 후 잠금 전부 해제 | 팝업 해제 말미에 오버레이 존재 여부로 배경 잠금 복원 |
| 40 | 분야 게이지 채움·트랙 대비 미달과 진행바 토큰 분기 | `app.css:255-256` · `:447-448` · `:259-260`(실측 §4-2, 토큰 종수 §4-5) | 채움을 단일 토큰으로, 트랙을 임계 이상으로(§4-5). 대비 헬퍼 재사용 단언 추가 |
| 41 | 수집 완료 표시가 텍스트 대비 미달 | `app.css:213` 의 `.btn:disabled` 감쇠(실측 §4-2) | 전용 클래스 + `aria-disabled` 로 텍스트 임계 확보. 중복 push 방지 분기 유지 필수 |
| 42 | 여정 잠금 카드 전체 감쇠 | `app.css:394` 가 카드 전체에 걸린다. 은회 4항목 전부 임계 미달(실측 §4-2) | 체크박스만 감쇠하고 잠금은 파선 테두리와 사유 텍스트로 표현. 차단룰에 화이트리스트 명시 |
| 43 | 히어로 두 버튼 위계가 테마 전환으로 뒤집힌다 | `app.css:206` 테두리가 테마 토큰이 아닌 고정값이며 남색 오버라이드 목록에 부재 | 테두리를 토큰으로 승격하고 양 테마에서 임계 통과 값으로 재산출. 차단룰 범위는 border-color 한정 |
| 44 | 분야별 진행 6행이 홈과 기록에 중복 렌더 | `app.js:475-486` 과 `:584-588` 이 동일 집계. 읽는 중·읽은 책도 같은 유형 2건 | 기록 탭 진행 블록과 조립부 제거. 차단룰은 순회 내 호출 위치 1회를 문자열 카운트로 판정 |
| 45 | 홈 한 화면에 여정 완료 수치 2회 표시 | `app.js:471` 과 `:511` 이 같은 두 값을 렌더하며 무스크롤 동시 가시 | 수치는 스트립에만 유지하고 카드 메타 줄은 수치 없는 안내로 교체. 숫자 서식은 일괄 적용 |
| 46 | 질문 46.8%가 원리 인용 템플릿 | `data/books.js:407-419` · `data/celeb-books-2025.js:176-180`. 상세에서 원문과 겹치는 책 166/175(94.9%) | 이번 범위는 시트 분리·접기만. 템플릿 제거는 대체 질문 집필과 한 묶음으로만 진행 |

### 10-4 하급 16건 — 백로그

이번 구현 대상이 아니다.

| 번호 | 제목 | 근거 요약 | 처방 요약 |
| --- | --- | --- | --- |
| 47 | 탭 전환 히스토리 무제한 누적 | `app.js:242-247` 조건 없는 pushState. 교차 12회에 인덱스 12, 종료까지 뒤로 13회 | 범위 제외. 구현 시 스택에 있는 탭이면 그 인덱스로 이동하는 정규화만 채택. 전면 replaceState 금지 |
| 48 | 방문자가 입력한 질문이 저장되지 않음 | `app.js:416` 모듈 변수이며 state 스키마에 슬롯 없음 | 백로그. 구현 시 최근 20건·각 60자 제한 + 정화 화이트리스트 등재. 입력창 값 복원은 넣지 않음 |
| 49 | 답변 상한 초과분이 무경고 절단 | `app.js:956-967` 이 절단만 하고 화면은 전문을 유지한다(초과 소실 실측 §5-2) | textarea 에 maxlength 부여(DI-3). 잔여 글자 상시 표시는 R6 위반이므로 붙여넣기 절단 시 1회 고지 |
| 50 | 홈 게이지로 건 서재 필터가 고착 | `app.js:891-896` 되돌림 코드 없음. 서재 재진입 시 필터 유지 | 요약줄에 전체 보기 되돌림 컨트롤. 탭 진입 시 무조건 초기화는 채택하지 않음 |
| 51 | 여정 체크박스 22px 로 터치 타깃 미달 | `app.css:396`. 375px 인터랙티브 35개 중 미달은 이 4개뿐 | 시각 크기는 유지하고 히트 영역만 44px 확보. 인접 카드 클릭 가림 여부를 실측 확인 |
| 52 | 답변 입력칸 3종 접근 이름이 placeholder 뿐 | `app.js:598` · `:609` · `:752` 의 접근 이름 출처가 placeholder 단일 | 질문 문단을 `aria-labelledby` 로 연결(id 부여 선행). 범위 제외 |
| 53 | rootArrivals 는 저장만 되고 노출 0 | `app.js:865` 증가 · `:101` 정화, 렌더 참조 0회. 재열람마다 중복 계상 | 기본안은 제거 + `tests/feature-regression.mjs:105` · `:108` 단언 동시 제거. 테스트 단독 삭제 금지 |
| 54 | font-size 선언 난립 | `app.css` 선언과 `app.js:766` 인라인이 6단 스케일을 벗어난다(종수·노드 분포 §4-3) | 6단 스케일로 수렴(§4-3). 히어로 계열은 원장 24에서 별도 처리 |
| 55 | 카드 표면 토큰이 순백 | `app.css:14`. 카드·탭바·검색 입력 전 표면에 적용 | 표면 값을 낮추고 원장 40의 트랙 조정과 함께 적용(§4-5) |
| 56 | 섹션 라벨 상단 간격 분기 | `app.css:112-118` 단일 선언인데 `.card` 가 button 이라 마진 접힘이 없다(실측 §4-6) | `.card + .section-label` 규칙 1줄로 수렴(§4-6). 카드 하단 마진은 건드리지 않음 |
| 57 | 죽은 상수·도달 불가 분기 3종 | `data/books.js:467` 로 `app.js:555` 분기 도달 불가 · `app.js:55` · `:99` version 미독 · `app.css:433` 미사용 | 시드 상수와 분기 삭제 후 분야 목표 정합 게이트 추가, version 필드 제거. 닫힘 버튼은 원장 38에서만 구현 |
| 58 | 내 서재 화면에 서재가 없다 | `app.js:777-797` 이 입력 폼과 안내 2줄만 렌더하고 기록 탭 링크 0건 | 원장 8 설계에 흡수. 수치 재표시 없이 기록 탭 이동 버튼 1개만 |
| 59 | 분야 편중과 가지의 줄기 건너뜀 | 문학 64권(36.6%) 대 역사 15권(8.6%). 가지 135권 중 부모가 뿌리인 것 76권(56.3%) | 부모 기준 그룹 렌더만 검토. 데이터 재배치는 정식 확장 시점으로 이월 |
| 60 | 콘솔 경고를 테스트 스텁이 은폐 | `app.js:373` `window.close()` 는 일반 탭에서 항상 차단되어 경고 3/3. `tests/mobile-navigation.mjs:358` 스텁 뒤 `:363` 이 오류 0을 단언 | 앱을 먼저 고쳐 경고를 없앤 다음 스텁을 제거(§6-7). 스텁 선삭제 금지 |
| 61 | 종료 닫기와 오버레이 닫기 라벨이 동일 | 오버레이 닫기 5곳과 `index.html:64` 가 모두 같은 라벨이며 구분 접근 이름이 없다 | 라벨 변경은 보류하고 원장 38의 복귀 경로 확보로 실질 피해를 없앤다 |
| 62 | 락파일·스크립트 매니저 불일치 | 락파일은 pnpm 단독인데 `package.json` 내부 호출은 npm, CI 는 pnpm. `.gitignore` 3줄. production-smoke 가 test 체인 밖 | 패키지 매니저 명시와 내부 호출 통일, `.gitignore` 3항 추가, production-smoke 편입. 방문자 영향 0이므로 범위 제외 |

### 10-5 추가 편입 5건 — 백로그

출처는 `docs/BACKLOG-v1.8-방문자실사.md` §3(2026-07-19 실사 잔여분)이며, 아래 근거는 2026-07-25 코드에서 결함 성립과 행 번호를 다시 확인한 값이다. 취급은 §10-4 와 같다.

| 번호 | 제목 | 근거 요약 | 처방 요약 |
| --- | --- | --- | --- |
| 63 | 수집한 질문을 철회·삭제할 수단 부재 | `app.js:881-888` 수집 분기가 `state.questions.push`(`:884`)만 수행하고 클릭 위임 선택자(`app.js:833`)에 삭제·취소 계열 `data-*` 0개. 기록 탭 문답집은 답변 textarea(`app.js:598`)만 렌더 | 문답집 항목에 개별 철회 컨트롤을 두고 위임 선택자에 등재. 실행 전 1회 확인은 C6-4 를 준용하고 히트 영역 44px 확보. 게이트는 수집 후 철회로 해당 id 가 `state.questions` 에서 사라지는 단언 1건 신설. 원장 8 은 전체 초기화·내보내기만 담아 이 경로를 대체하지 않음 |
| 64 | 여정 상세 시트에 순서 규칙·체크 의미 안내 부재 | `renderJourneyDetail`(`app.js:726-774`)에 순서 규칙·잠금 사유 텍스트 0개. 잠금 표현은 `app.js:737` `is-locked` 와 `:738` `disabled` 뿐이고 체크박스 접근 이름은 `"${title} 읽음 체크"` 단일. `renderJourneyList` 의 `.notice`(`app.js:719`)는 진행 중 여정이 있을 때만 노출 | 시트 상단 `.q-card` 아래에 순서 규칙 1문장을 상시 노출하고 잠금 카드에는 사유 텍스트를 붙인다(원장 42가 신설하는 사유 노드와 같은 노드를 쓴다). 안내는 시트당 1곳만 두고 카드마다 반복하지 않는다(R6) |
| 65 | 여정 전체 완주 인정 문구 부재 | `renderJourneyList`(`app.js:701-724`)는 6종 전부 완료 시 `app.js:707` 이 모든 카드를 `disabled` 로, `:708` 이 `· 완료` 로만 표시하고 전체 완주 분기가 없다 | `state.journeysDone` 종수가 `JOURNEYS.length` 와 같을 때 완주 문구 1블록을 목록 상단에 렌더. 완주 수치는 홈 스트립이 원천이므로 문구에 수치를 넣지 않는다(R6, 원장 45와 동일 원칙) |
| 66 | 종료 팝업 배경 탭 무반응 | `index.html:59` `#exit-dialog`(`.exit-backdrop`)에 리스너 0건이고 처리 경로는 `app.js:329` 계속 읽기 · `:372` 닫기 · `:333` ESC 뿐. 앱 내 시트는 `data-close-overlay` 배경(`app.js:714` · `:760`)으로 닫히므로 관례가 어긋난다 | 배경 탭을 ESC 와 같은 `stayAtHome` 경로에 연결한다(취소 방향으로만 동작). 배경 요소 자체 클릭과 자식 버블링을 구분해 닫기 버튼 클릭이 이중 처리되지 않게 한다. 원장 39의 배경 잠금 복원과 같은 해제 말미를 공유. 게이트는 배경 좌표 클릭 후 팝업 비표시·포커스 복귀 단언 |
| 67 | manifest 설치 품질 — maskable 아이콘·최상위 id 부재 | `manifest.webmanifest:12-16` icons 3항목 중 `purpose: "maskable"` 0건, 최상위 `id` 키 없음. 현행 게이트는 192·512 존재만 확인한다(`tests/static-rules.mjs:40-41`) | 512 아이콘에 `maskable` 용도 항목을 추가하고 `id` 를 `start_url` 과 같은 범위 값으로 고정. `tests/static-rules.mjs:36-41` 에 두 단언을 추가한다. 안전 영역을 반영한 아이콘 비트맵 재작업은 범위 외 |

---

## §11 기계 채점 게이트

### 11-1 기존 게이트 지뢰 목록

현행 게이트 중 개선 대상인 현행 구현을 강제하는 조항 전량이다. 지뢰의 성격과 처방 원칙은 §9 E-003 이 단일 출처이고, 편집 파일별 확인 대상 대응은 §0-3 표가 단일 출처다. 이 표는 조항 단위로 무엇이 강제되고 어떤 수정이 그것을 깨는지를 다룬다. 파일명은 모두 `tests/` 하위이며 행 번호는 실제 파일을 열어 확인한 값이다.

| 조항 | 강제하는 것 | 이것을 깨는 수정 | 동반 갱신 방법 |
| --- | --- | --- | --- |
| `static-rules.mjs:26-29` | `class="tab"` 4개와 4탭 문서 순서 | 탭 추가·삭제·재배열 | INV-4 이므로 변경 금지. 승인 시 두 단언 동시 개정 |
| `static-rules.mjs:31-34` | 홈 정보 순서 표식 4개(`${questionSearchHtml()}` · `${lastQObj ? \`` · 질문 여정 라벨 · 분야별 진행 라벨)의 소스 등장 순서 | 원장 7 안내 카드 삽입, 원장 44 진행 블록 제거, 홈 템플릿 리팩터, 라벨 문구 변경 | 히어로 직후 삽입은 통과한다. 라벨 문구를 바꾸면 배열 리터럴을 같은 커밋에서 교체 |
| `static-rules.mjs:36-41` · `:43-50` | manifest 4필드·아이콘 2종·title·메타 4종·구조화 데이터 이름 문자열 | 앱 이름·부제 변경, 메타 정리 | INV 대상이므로 변경 금지 |
| `static-rules.mjs:52-56` | `sw.js` 문자열에서 추출한 자산의 실존과 필수 5종 포함 | 자산 추가·경로 변경·파일 분할 | 새 자산은 `sw.js` 목록과 필수 배열을 같은 커밋에서 갱신 |
| `static-rules.mjs:57` | 캐시 버전 문자열 하드코딩 | 모든 배포 커밋 | §0-3 3항. 버전 상향과 동시 갱신 |
| `static-rules.mjs:58` | SW 등록 호출 문자열 완전일치 | 등록 코드 리팩터, 옵션 추가 | 정규식을 새 호출 형태로 교체 |
| `static-rules.mjs:59` | `isUpdate` · `clients.matchAll` · `client.navigate` 3키워드 순서 존재 | 원장 12 처방(navigate 제거 후 postMessage 통지) | 규칙을 postMessage 통지 존재로 교체하고 navigate 부재를 단언 |
| `static-rules.mjs:63` · `:64` | 그라데이션·이모지 0건(html·app·css 합본) | 새 문구·새 스타일 | 위반 시 수정 대상은 산출물. 규칙 완화 금지 |
| `static-rules.mjs:65` | fetch·XMLHttpRequest·axios 0건 | 네트워크 코드 도입 | INV-3 이므로 금지. `Blob` 과 `a[download]` 은 무영향 |
| `static-rules.mjs:66` | 인증 어휘 5종 0건 | 원장 7 · 8 의 새 안내 문구 | §2-4. 문구 작성 전에 확인 |
| `static-rules.mjs:67-68` | 히어로 강조색 대비를 색 리터럴 4개로 계산 | §4-1 토큰 값 변경 | 토큰을 바꾸면 리터럴 4개를 같은 커밋에서 교체 |
| `static-rules.mjs:69` | `#theme-btn` 에 `aria-pressed` 부재 | 원장 8 의 테마 토글 설정 이관 | 버튼이 사라지면 단언 대상을 새 컨트롤로 이전 |
| `static-rules.mjs:72` | 프로필을 `textContent` 로 출력 | 프로필 렌더 리팩터 | INV-7 이므로 표현만 유지 |
| `static-rules.mjs:73` | `myAnswer` 정화에 `slice(0, 10000)` 리터럴 등장 | DI-3 상한 상수화 | 정규식을 상수 참조 형태로 교체하고 상수 정의를 함께 단언 |
| `static-rules.mjs:76-79` | 브랜드 버튼 HTML 완전일치 + `.brand-btn` 44px 2선언 | `index.html:27` 마크업 한 글자 변경, 클래스 재구성 | 템플릿 문자열을 같은 커밋에서 교체. INV-6 유지 |
| `static-rules.mjs:80-83` | `goHome` 정의 · `history.go(-d)` · 브랜드·홈탭 클릭 분기 | 원장 14 히스토리 재설계가 `history.go(-d)` 를 대체할 경우 | 복귀 구현을 바꾸면 `:81` 을 새 진입 형태로 교체. INV-6 은 유지 |
| `static-rules.mjs:86-88` · `:89-94` | `stepsToRoot` 단일 정의, 스트립 2·3칸 정규식 창(각 120자·140자), `is-ctx` 정확히 2회 | 원장 34 의 분야 속성 추가, 원장 33 의 라벨 분리, 스트립 마크업 재배치 | 속성은 창 안에 두거나 창 길이를 같은 커밋에서 조정 |
| `static-rules.mjs:95-97` | reduced-motion 미디어쿼리 안의 전환 효과와 120ms 이내 지속시간 | 애니메이션 추가·제거 | §8-1 을 유지하며 규칙 대상 선택자만 교체 |
| `feature-regression.mjs:76-81` | `.view > .card` 카운트 80 · 160 · 175 와 더 보기 라벨 문자열 | 원장 1 2단계의 `#lib-list` 컨테이너 분리, 카드 래핑 변경 | 선택자를 새 컨테이너 기준으로 교체 |
| `feature-regression.mjs:84-98` | 시트 모달 속성·배경 aria-hidden·트랩·ESC 후 호출 위치 포커스 복귀 | 오버레이 렌더·닫힘 경로 재설계(원장 4 · 20) | 단일 닫힘 함수 도입 후에도 같은 결과가 나와야 한다. 단언 유지 |
| `feature-regression.mjs:101-110` | 뿌리 도달 카운터가 동작당 1회만 증가 | 원장 53 의 카운터 제거 | 필드 제거 시 두 단언을 같은 커밋에서 삭제 |
| `feature-regression.mjs:112-121` | 읽음 3단 순환과 저장 반영 | 원장 30 의 읽음 출처 기록 도입 | 사용자 지정 읽음의 순환은 불변으로 유지 |
| `feature-regression.mjs:131-135` | 답변 지연 저장과 `#record` 해시 reload 후 값 복원 | 원장 6 해시 부팅 복원, 원장 3 저장 시점 변경 | 해시 부팅 후에도 같은 값이 나오는지 확인. 저장 디바운스 변경 시 대기 시간 재산정 |
| `feature-regression.mjs:141-148` | `[data-jcheck]` 4개와 disabled 배열, 해제 시 이후 진척 제거 | 원장 9 부분 갱신, 원장 30 원복 처방 | 부분 갱신 후에도 배열이 같아야 한다. 진행 초기화 규칙은 바꾸지 않는다 |
| `feature-regression.mjs:150-157` | `#j-answer` 입력이 완료 기록의 답변으로 저장 | 원장 9 의 draft 승격과 id 변경 | 입력 요소 id 를 유지하고 draft 경로가 같은 결과를 남기게 한다 |
| `feature-regression.mjs:163-190` | 정화 결과를 삭제 방식으로 직접 단언(`:179` 버전, `:180-181` 읽음·읽는 중, `:182-184` 질문 1건과 상한, `:185-186` 여정, `:187` 테마, `:188-189` 이스케이프) | 원장 11 의 격리 보존 전환, DI-2 안정 qid 도입, §12-3 의 version 필드 제거 | 격리 전환과 동시에 기대값을 격리 결과로 교체. version 제거는 `:179` 삭제와 한 커밋 |
| `feature-regression.mjs:195-204` | Quota 주입 후 앱 동작 유지와 `#app-status` 실패 문구 정규식 | 원장 10 의 통보 경로 교체 | 기존 통보를 유지하고 배너를 추가만 한다 |
| `mobile-navigation.mjs:129` · `:131` · `:135-136` | 해시 없는 진입의 최종 URL, 첫 탭, 히어로·상단바 배경 RGB 하드코딩 | 원장 6 해시 복원, §4-1 토큰 값 변경 | 해시 없는 진입 결과는 보존. 색 변경 시 RGB 기대값 동시 교체 |
| `mobile-navigation.mjs:133` · `:388` | 가로 넘침 0(100% 기준, 390px 과 3폭) | 스트립 폰트 확대(원장 54), 히어로 박스 변경(원장 25) | 확대 폭 게이트를 신설로 추가하고 기존 단언은 유지 |
| `mobile-navigation.mjs:137-142` · `:389-394` | 가시 버튼 전량 44x44px | 원장 7 · 8 의 신규 버튼, 원장 51 히트 영역 조정, 원장 38 닫힘 버튼 | 신규 컨트롤은 처음부터 임계 준수. 감사 자체는 개정하지 않는다 |
| `mobile-navigation.mjs:144-149` | 재로드 시 직전 히어로 질문 반복 금지 | 원장 3 의 로드 시점 저장 제거 | pagehide 확정과 동시 적용해야 확률 의존이 되지 않는다 |
| `mobile-navigation.mjs:152-160` | 카탈로그 3수치 완전일치 | 원장 2 · 46 문구 재생성, 원장 59 데이터 재배치, 도서 추가 | INV-8. 데이터 변경과 같은 커밋에서 기대값 교체 |
| `mobile-navigation.mjs:162-190` | 질문 전수 2줄 이하. `:180` 에 글자수 버킷 22 · 29 · 40 과 클래스 매핑이 하드코딩 | 원장 24 3단 축소, 원장 25 폭 반영, 원장 2 문구 길이 변경 | §4-4 임계를 단일 출처로 추출해 앱과 테스트가 같은 값을 참조하게 한다 |
| `mobile-navigation.mjs:192-202` | 테마 버튼 라벨·접근 이름·재로드 후 유지 | 원장 8 의 설정 이관 | 컨트롤 이전 시 단언 대상을 새 위치로 옮긴다 |
| `mobile-navigation.mjs:204-208` · `:210-213` | 홈 재탭 후 스크롤 0, 게이지 진입 후 서재 필터 결과 | 원장 19 스크롤 초기화, 원장 50 필터 되돌림 | 두 단언을 통과시키는 범위에서만 처방 적용 |
| `mobile-navigation.mjs:217-220` | 장문 질의의 결과 존재와 특정 도서 노출 | 원장 15 · 18 점수·사전 변경 | 검색 변경 후 이 단언과 `search-quality.mjs` 를 함께 재실행 |
| `mobile-navigation.mjs:222-233` | 탭 백 시퀀스에서 중간 단계 팝업 비표시와 첫 화면 뒤로가기 팝업 표시 | 원장 13 · 14 페이로드 재설계, 원장 36 히스토리 확장 | §6-2 와 §6-3 을 동시에 만족시킨다. 히스토리 항목 수가 바뀌면 시퀀스 재조정 |
| `mobile-navigation.mjs:234` · `:236` · `:238-245` | 팝업 초기 포커스, 배경 aria-hidden, 트랩 2방향, ESC 복귀 후 해제 | 원장 39 배경 잠금 복원 | 오버레이 없는 경로에서 같은 결과가 나오게 한다 |
| `mobile-navigation.mjs:247-276` | 프로필 시나리오 전체 — `:249-250` tel 입력·인증 어휘 0건, `:252` 오류 가시, `:257` · `:263` · `:267` · `:271` 라벨 문자열, `:272` 삭제 결과 | 원장 26 말줄임, 원장 27 form 전환, 원장 28 오류 문구 주입, 원장 8 삭제 범위 확대 | 라벨 문자열은 그대로 유지한다. 오류 표시는 빈 상태 높이 0을 지켜야 `:252` 가 정상 동작한다 |
| `mobile-navigation.mjs:279-299` | 스트립 `is-ctx` 위치 배열, 문맥값 일치, 라벨 2종, 링크 대상 2종, 뿌리까지 값 형식 | 원장 33 라벨 분리, 원장 34 페이로드 추가 | 라벨·대상·형식을 유지하고 그룹 접근 이름만 조정 |
| `mobile-navigation.mjs:302-350` | 브랜드·홈탭·시트 경유 3경로의 첫 화면 복귀와 인덱스 0, 이후 뒤로가기 팝업, 시트 열림 중 상단바 잠금 | 원장 13 · 14 페이로드 재설계, 원장 4 닫힘 단일화 | 인덱스 필드를 유지하고 뷰 직렬화를 추가한다 |
| `mobile-navigation.mjs:352-356` | 팝업 표시 중 반복 뒤로가기에서 팝업 유지·URL 불변 | 센티널 값 식별 전환(원장 13) | INV-10. 전환 후 재실행 필수 |
| `mobile-navigation.mjs:358-363` | `window.close` 스텁 후 닫힘 화면 가시·문구·런타임 오류 0 | 원장 60 스텁 제거, 원장 38 버튼 추가, 원장 61 라벨 변경 | 앱 수정 후 스텁 제거. 문구와 종료 버튼 id 는 유지 |
| `data-integrity.mjs:11-16` | 인생책 시트 집계 4수치와 도서 175권·id 유일 | 데이터 추가·병합 규칙 변경 | 데이터 변경과 같은 커밋에서 기대값 교체 |
| `data-integrity.mjs:27-29` | 책마다 질문 3개 이상·출처 존재·질문 44자 이하 | 원장 2 의 어절 절단과 말줄임표 부착(길이 증가) | 재생성 후 전량 재실행. 초과 시 접미 길이 재산정 |
| `data-integrity.mjs:30-44` | tier 와 roots 정합, 순환·고아 금지 | 데이터 재배치(원장 59) | INV-2. 규칙 유지 |
| `data-integrity.mjs:62-74` | 여정 6종·중복 금지·4권에서 5권·첫 책 뿌리·계단 비역행 | 원장 11 의 결번 건너뛰기, 여정 구성 변경 | 데이터 규칙은 유지하고 런타임 정화만 바꾼다 |
| `data-integrity.mjs:78-81` | 분야 목표 순서와 합계, 현재 수록량이 목표 이하 | 분야 추가, 목표 재배분 | INV-8. 승인 후 동시 개정 |
| `data-integrity.mjs:85-86` | 질문 문구 중복 금지, 일괄 생성형 문구 부재 | 원장 2 대체 문구 집필, 원장 46 템플릿 정리 | 대체 문구에 책 고유 요소를 포함해 중복을 회피 |
| `search-quality.mjs:6-27` | 벤치마크 질의별 상위 5권 포함·상위 3권 분야 | 원장 18 주제 그룹 확장이 분야 가중을 바꿈 | 그룹 추가 후 전량 재실행. 기대값 조정은 근거를 §9 에 남긴다 |
| `search-quality.mjs:29-30` · `:37` | 짧은 입력·핵심어 없는 질의·의문 조각의 빈 결과 | 원장 15 완전일치 보너스, 원장 17 UI 하한 완화 | 엔진 조기 반환 경로를 유지하면 무영향(실측 확인). UI 하한만 바꾼다 |
| `search-quality.mjs:33-42` | 2자 핵심어 결과와 조사 정규화 동일 결과 | 원장 18 JOSA 확장 | 확장 후 재실행. 정규화 동일성은 유지 |
| `search-quality.mjs:46-57` | 부분문자열 오탐 금지(1자 앵커와 2자 이상 어휘 분리)와 분야 상위 3권 | 원장 18 JOSA·그룹 확장 | 1자 앵커 3종 외에는 앵커가 아니므로 안전. 재실행으로 확인 |
| `search-quality.mjs:59-71` | 철학 정전 최상위, 인식론 질의 비공백, 1자 주제어 결과 존재 | 원장 15 보너스, 원장 18 그룹 확장 | 1자 주제어 단언은 원장 17 UI 처방의 근거이므로 유지 |
| `production-smoke.mjs:56` · `:80` · `:85` | 운영 응답의 제목, 히어로 배경 RGB, 운영 캐시 버전 문자열 | 배포 커밋 전량, §4-1 토큰 변경 | `npm test` 체인 밖(`package.json` `test:production`)이라 자동으로 잡히지 않는다. 배포 전 별도 실행하고 §12-1 5항에 포함 |

### 11-2 신설 예정 게이트 명세

2차에서 작성할 규칙이다. 각 규칙은 통과 조건이 기계 판정 가능한 단언으로만 기술되며, 배정 파일 원칙은 다음과 같다. 정적 문자열·색 리터럴은 `static-rules.mjs`, 상태·저장 왕복은 `feature-regression.mjs`, 실기기 좌표·히스토리·IME 는 `mobile-navigation.mjs`, 데이터 전수는 `data-integrity.mjs` 다.

| 규칙 | 채점 문장 | 배정 파일 | 대상 원장 |
| --- | --- | --- | --- |
| G-1 IME 조합 하드게이트 | CDP `Input.imeSetComposition` 3단계로 2음절 한글을 조합한 뒤 `#lib-search` 값이 조합 결과와 정확히 같고, 결과 카드 1장 이상이며, compositionend 가 1회 발생한다 | `mobile-navigation.mjs` | 1 |
| G-2 여정 완료 이중 클릭 단일 기록 | 동일 태스크 2회 호출과 실포인터 더블탭 0 · 80 · 150 · 250ms 각각에서 완료 기록 증가가 1건이고, 종료 팝업 비표시이며, 현재 탭이 유지되고 다른 여정이 시작되지 않는다 | `mobile-navigation.mjs` | 4 |
| G-3 초안 보존 | 완료 답 초안 입력 후 체크박스 해제와 재체크를 거쳐도 입력 요소 값과 저장된 초안이 입력 문자열과 같다 | `feature-regression.mjs` | 9 |
| G-4 prefs 복원 | 계보 분야 칩과 서재 계단 칩을 각각 비기본값으로 선택한 뒤 reload 하면 두 칩의 `aria-pressed` 가 true 이고 서재 요약 권수가 선택값과 일치한다. 검색어는 복원되지 않는다 | `feature-regression.mjs` | 5 |
| G-5 저장 실패 시 낙관적 UI 금지 | Quota 주입 상태에서 수집을 실행하면 버튼 라벨이 완료 표시로 바뀌지 않고, 가시 `[role="alert"]` 가 1건 존재하며, 기존 라이브 리전 문구도 유지된다 | `feature-regression.mjs` | 10 |
| G-6 카탈로그 격리 보존 | 기록 보유 도서를 제거한 카탈로그로 로드하면 격리 항목 수가 잃어버린 참조 수와 같고, 읽음·질문·답변 원본이 삭제되지 않으며, 기록 탭 고지가 1회 노출된다 | `feature-regression.mjs` | 11 |
| G-7 히스토리 페이로드 왕복 | 홈에서 기록까지 진행한 뒤 뒤로 2회·앞으로 3회에서 URL 변경 횟수와 렌더 탭 변경 횟수가 같고, 앞으로가기 후 렌더 탭이 해시 대응 탭이며, `history.state` 에 뷰가 직렬화되어 있고 센티널은 고유 값으로 식별된다. 해시 딥링크 진입 시 렌더 탭과 최종 URL 이 요청 해시와 일치한다 | `mobile-navigation.mjs` | 6 · 13 · 14 |
| G-8 폰트 선언 6종 제한 | `app.css` 의 `font-size` 선언 집합이 §4-3 6값에 포함되고, `app.js` 와 `index.html` 의 인라인 `font-size` 가 0건이며, 히어로 계열 선언은 3종이고 최솟값이 §4-4 하한과 같다 | `static-rules.mjs` | 24 · 54 |
| G-9 비텍스트 대비 | 진행바 채움과 트랙, 트랙과 카드, 버튼 테두리와 면 조합 전량이 두 테마에서 §8-1 비텍스트 임계 이상이다. 색 리터럴은 `static-rules.mjs:15-24` 헬퍼로, 실렌더 계산색은 4탭과 오버레이 5종 렌더로 채점한다 | `static-rules.mjs` + `mobile-navigation.mjs` | 40 · 42 · 43 · 55 |
| G-10 인용 질문 종결 | 질문 문구가 원문 principle 의 접두이면 반드시 말줄임표로 끝난다. 어절 중간 절단 항목이 0건이다 | `data-integrity.mjs` | 2 |
| G-11 병합 저장·다중 탭 | 입력 0회 첫 로드에서 저장 키가 생성되지 않고, 두 컨텍스트가 번갈아 저장한 뒤 최종 저장값에 두 컨텍스트의 항목이 모두 남으며, `storage` 수신 후 렌더 결과가 저장값과 일치한다. 병합 후 `version` 은 현행 값이다 | `feature-regression.mjs` | 3 |
| G-12 첫 방문 1회 고지 | 기록 0건 첫 로드에서 등록 안내가 정확히 1건이고 히어로 직후에 있으며, 해제 후 reload 에서 0건, 기록 보유 상태 첫 로드에서 0건이다. 문구는 §2-4 금지 어휘 0건이며 홈 정보 순서 표식을 깨지 않는다 | `feature-regression.mjs` + `static-rules.mjs` | 7 |
| G-13 설정 진입점·기록 초기화 | 설정 오버레이 진입점이 1개이고, 초기화 실행에 확인 1회가 필요하며, 실행 후 read·reading·questions·journey·journeysDone·profile 이 전부 비고 직후 저장에서 되살아나지 않는다. 내보내기·불러오기 경로에 외부 요청이 0건이다 | `feature-regression.mjs` | 8 |
| G-14 배포 갱신 통지 | `sw.js` 에 `client.navigate` 가 0건이고 갱신 통지가 postMessage 로만 나가며, 캐시 버전을 올린 워커가 활성화된 뒤에도 열린 탭의 `history.state` 가 보존되고 종료 팝업이 비표시이며 URL 과 렌더 탭이 일치한다 | `static-rules.mjs` + `mobile-navigation.mjs` | 12 |
| G-15 계보 진행률 탭·첫 페이지 복귀 | (a) 서재 최하단 스크롤 후 계보 탭 클릭 → `scrollY === 0` (b) 같은 탭 재탭 → `scrollY === 0` (c) 서재 더 보기 후 다른 탭을 거쳐 서재 탭 → 카드 수 = 첫 페이지 수 (d) 기록 첫 진입에서 `aria-expanded="true"` 0건, 분야 탭 6개, 한 분야를 누르면 그 패널 목록 행 수 = 그 탭의 진행률 분모이고 동시 펼침은 1개이며 재탭이면 접히고 액션 직후 `activeElement !== BODY` 이며 `.progress-row`·`.progress-book` 44px 미달 0건이고 목록 행 탭이 책 상세를 연다 (e) 기록 탭 재탭 → 펼친 목록 0건 · `scrollY === 0` | `mobile-navigation.mjs` + `static-rules.mjs` | 신규(2026-08-02 지시) |

### 11-3 1차 범위 제한과 마감 차단 원칙

1차에서는 `tests/*.mjs` 와 `package.json` 을 편집하지 않는다. 11-2 는 명세이며 구현이 아니다.

규칙만 있고 게이트가 없으면 반복된다. 2차의 각 라운드는 `npm test` 전량 통과 없이 머지를 금지한다.

---

## §12 마감 체크리스트

### 12-1 순서 게이트

아래 7항을 순서대로 통과한다. 앞 항이 미통과인 상태에서 뒤 항으로 넘어가지 않는다.

1. **계획.** 라운드 착수 전에 §10-1 배정과 §11-1 지뢰를 조회한다. 착수 보고에 문서 숙독 1줄, 스킬 매핑 1줄, 슬롯 기준 예상 벽시계 1줄(§9 E-001)을 포함한다.
2. **구현.** 라운드 안에서 편집 파일 교차 0을 지킨다. `app.js` 는 한 라운드 1기다.
3. **1:1 매칭 검증.** 12-2 표를 작성한다. 원장 항목 수와 처리 항목 수가 다르면 마감을 차단하고 완료를 보고하지 않는다.
4. **`npm test` 전량.** 5종 전부 통과(§0-3 1항). 배포를 포함하는 라운드는 `test:production` 을 추가로 실행한다(§11-1 마지막 행).
5. **CI quality.** `.github/workflows/quality.yml` 통과. 로컬 통과만으로 마감하지 않는다.
6. **PR.** 본문에 라운드 번호, 처리한 원장 번호 목록, 갱신한 게이트 목록, 상향한 캐시 버전을 기재한다.
7. **정리.** 12-3 대상을 처리하고 처리·보류를 항목별로 밝힌다.

### 12-2 1:1 매칭 대조표 양식

라운드 종료 보고에 이 표를 그대로 포함한다. 상태는 반영·보류·범위외 3값만 쓴다. 상급 14건 전량이 반영이 되기 전에는 2차 마감을 보고하지 않는다.
보류·범위외 항목은 사유 1행과 재개 조건 1행을 함께 적는다. 사유 없는 보류는 누락으로 판정한다(§9 E-008).

**2차 최종 대조표 (2026-07-25 마감).** 행 번호는 탐색 힌트이며 근거가 아니다(§9 E-009) — 식별자로 인용한다.

| 원장 | 처방 반영 위치 | 동반 갱신한 게이트 | 상태 |
| --- | --- | --- | --- |
| 1 | `app.js` 서재 검색 조합 가드 + `compositionend`(1단계) · `renderLibList` 컨테이너 분리(2단계) | G-1 + 입력 노드 동일성 · `#lib-list > .card` 선택자 8곳 | 반영 |
| 2 | `data/books.js` `ensureQuestionDepth` · `data/celeb-books-2025.js` `principleQuoteQuestion` 어절 경계 + 말줄임표 | G-10 신설 | 반영 |
| 3 | `app.js` `save` 읽기·병합·쓰기 + `storage` 리스너 + 첫 방문 무기록 | G-11 | 반영 |
| 4 | `app.js` `dismissOverlay` 단일화 · 재진입 플래그 · 중복 가드 · 복원 시트까지 관통 차단 · 여정 그만두기 | G-2 5모드 + `tapThroughReleaseGate` | 반영 |
| 5 | `app.js` `sanitizePrefs` + `prefs{lineageDomain,libDomain,libTier}` 영속·복원 | G-4 | 반영 |
| 6 | `app.js` 진입 해시 역매핑 2항목 부팅(N-10) | G-7 딥링크 절 | 반영 |
| 7 | `app.js` 히어로 직후 1회성 안내 카드 + 해제 상태 영속 | G-12 | 반영 |
| 8 | `app.js` `settings` 오버레이 + 인메모리 재초기화 · `index.html` 진입점 | G-13 | 반영 |
| 9 | `app.js` `journeyDraft` 승격 + 체크박스 부분 갱신 + sr-only label | G-3 | 반영 |
| 10 | `app.js` `commit()` 롤백 + 가시 배너(라이브 리전 유지) | G-5 | 반영 |
| 11 | `app.js` `orphans` 격리 + 안정 qid + 여정 결번 건너뜀 + 기록 탭 고지 | G-6 + 정화 기대값 교체 | 반영 |
| 12 | `sw.js` `postMessage` 통지(캐시 v1.9.0) · `app.js` 안전 시점 확인 후 reload | G-14 static·실기기 양측 | 반영 |
| 13 | `app.js` 센티널 `{sentinel:true}` 값 식별 + 미지 state 해시 우선 재구성 | G-7 | 반영 |
| 14 | `app.js` `{i, view}` 직렬화 + 잘라내기 제거(포인터 이동) | G-7 왕복 | 반영 |

**미이행 2건(사유·재개 조건).**

- DI-3 답변 상한 상수화 — `tests/static-rules.mjs` 가 상한을 리터럴 정규식으로 강제한다. 값은 동일하고 동작 차이는 없다. 재개 조건: 그 정규식을 상수 참조 형태로 교체하는 커밋에서 상수 도입을 함께 진행한다.
- 원장 3 의 "히어로 확정을 날짜 변경 시로도 한정" — `tests/mobile-navigation.mjs` 가 재로드마다 직전 히어로 질문 반복 금지를 요구하므로 날짜 고정과 양립하지 않는다. 채점되는 동작은 `pagehide` 확정과 동일하므로 필드만 늘면 R6 위반이다. 재개 조건: "오늘의 질문 1일 1문 고정" 이 INV 로 승인되면 해당 단언과 같은 커밋에서 교체한다.

**독립 검증 범위.** P4 적대적 검증(대조표 재구성 · 게이트 뮤테이션 · Flow 실측 · 만족도 재측정 · 회귀 탐색 · 완결성 비평 6기)은 착수 직후 사용자 판단으로 중단했다. 따라서 마감 근거는 (a) `npm test` 5종 전량 초록 (b) R3 검증기의 상급 14건 브라우저 재현 재대조(해소 13 · 부분해소 1, 잔여는 R4 가 닫고 G-2 5모드로 확인) 두 가지다. **게이트의 채점력 자체는 뮤테이션으로 증명되지 않았다** — 다음 작업의 최우선 항목이다.

### 12-3 정리 대상

삭제 판정은 §9 E-007 절차를 따른다. **2026-07-25 2차 마감 시 1·2·3·7·12 처리 완료**, 나머지는 해당 원장이 중·하급이라 백로그로 남았다.

1. ~~`graphify-out/`~~ — 삭제 완료(238KB).
2. ~~`test-results/`~~ — 디스크에 없음(생성 이력 없음).
3. ~~`playwright-report/`~~ — `.gitignore` 등재 완료. 디스크에 없음.
4. `data/books.js` 시드 상수와 이를 참조하는 `app.js` 도달 불가 분기(원장 57) — 미처리(하급).
5. `app.js:55` 저장 버전 상수와 `app.js:99` version 필드 — `loadState` 가 읽지 않는 미사용 마이그레이션 필드. `tests/feature-regression.mjs:179` 단언을 동반 삭제. DI-6 을 채택하면 제거 대신 마이그레이션 함수를 둔다.
6. `app.js:101` · `:865` 뿌리 도달 카운터 증가·정화 코드와 `tests/feature-regression.mjs:105` · `:108` 단언(원장 53).
7. `app.js:571-576` 검색 입력 선택 위치 저장·복원 코드 — 원장 1 근본 처방 채택 시 불필요.
8. `app.css:433` 닫힘 화면 버튼 스타일 — 원장 38로 실사용 전환하거나 삭제. 둘 중 하나로 정리한다.
9. `tests/mobile-navigation.mjs:358` 창 닫기 스텁 — 원장 60 앱 수정 후 제거.
10. `app.js:584-588` 과 `:613-615` 기록 탭 분야별 진행 중복 블록(원장 44).
11. `docs/BACKLOG-v1.8-방문자실사.md` §3 미편입 5건 — 편입 완료(원장 63부터 67까지, §10-5). 남은 작업은 그 문서 §3 각 항목에 원장 번호를 병기해 미편입 표기를 해제하는 것뿐이다.
12. 스크래치패드 프로브 산출물(§0 서두 경로 접두 아래 d1부터 d8까지의 프로브·스크린샷·JSON) — 저장소 외부이며 마감 후 삭제.

### 12-4 자기점검

마감 보고 직전에 4문 전부에 예로 답할 수 있어야 한다. 하나라도 아니면 마감하지 않는다.

1. 이번에 받은 지시와 교정을 이 문서의 해당 절로 규칙화했는가(R1 · R2).
2. 이번에 겪은 오류를 전부 §9에 등재했는가(R3).
3. 산출물이 §0-3 마감 게이트와 §11 게이트를 통과했는가(R4).
4. 같은 수치·라벨·문장을 두 번 표시하지 않았는가(R6 · §0-4).
